# GENERATED by agentic-sdlc — `agentic-sdlc install-ci`.
#
# One job: check out, ARM the checkout, run this project's full gate. It
# assumes `make milestone` is that gate; a project without the target gets a
# red step naming it. Your toolchain is yours to add below, guarded on a
# tracked file that only exists where it is wanted, the way the arming step
# is. After this file is written it is YOURS: edit it here; re-installing
# needs --force, and `--diff` shows what a re-install would change.
name: verify

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # The PM and doc gates read worktrees, branches and tracked files.
          fetch-depth: 0
      - name: Arm the tracked git hooks
        # `core.hooksPath` is repo-local config a fresh checkout never has.
        if: hashFiles('tools/setup-hooks.sh') != ''
        run: bash tools/setup-hooks.sh
      - uses: astral-sh/setup-uv@v5
      # --- your toolchain goes here -----------------------------------------
      #   - name: Install the linters this project's gate shells out to
      #     if: hashFiles('<a file only this kind of project has>') != ''
      #     run: |
      #       uv tool install "<linter>"
      #       sudo apt-get update -qq && sudo apt-get install -y -qq shellcheck
      # ----------------------------------------------------------------------
      - name: make milestone
        # CI has no second look, so the gates stream.
        env:
          VERBOSE: "1"
        run: make milestone
      - name: keep the gate transcripts of a red run
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: gate-reports
          path: .gate-reports/
          if-no-files-found: ignore
