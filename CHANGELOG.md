# Changelog

## Unreleased

- **`make smoke` asserts that canonicalize invents no `index=`, over EVERY tracked scene on both consumers** — the `canonicalize round trip` row is pinned to one scene per consumer and stays pinned, because a created node's authored `index=` is not derivable and trail's inherited scenes disagree with each other about whether the attribute is even written. But the INVENT direction is true of all 310 scenes today, so it is now asserted over all 310: degrade each the way `save()` does, canonicalize, and count the attributes that came back on a node that never had one. Live: `trail 116 scene(s): 0 invented, 5/17 authored index= restored, 12 not derivable (10 scene(s) not index-identical)` and `nullbound 194 scene(s): 0 invented, 39/39 authored index= restored, 0 not derivable (0)`. The round-trip counts ride in the detail of a passing row, reported and never gated — pinning them would be the known-fail list this package refused to write, and the last census of them was wrong within a day of being taken. This row exists because `0.24.0/bugs/index-is-derivable-under-an-instanced-parent` proposed keying restoration off "the node's PARENT is an instanced subtree" rather than off `type=`/`instance=` on the node itself. That was measured before it was refused, and the row catches every form of it: the filed rule invents 500 attributes across the two trees and takes nullbound from 0 round-trip failures to 26; the narrowest form — inherited scenes only, parent placed by the base — still invents 4, all of them the `force_resolution.tscn`/`game_over.tscn` nodes the bug itself named as its stop signal. The ORDINAL is derivable; whether the engine emits the attribute is not, and the two are different claims. `scene_canonicalize` keeps writing nothing there, with the census as its stated reason: nullbound is 194 editor-written scenes with 1008 created nodes and not one `index=` among them, while trail is hand-authored throughout and splits 10-for/10-against at the one position in dispute. Seven cases in `tests/test_canonicalize.py::ACreatedNodeGainsNoIndexWhateverItsParentIs` pin the shapes — under an instance node, under a base child, into an empty base container, under a locally created parent, and a run of created siblings that must gain no run of ordinals — each proven by mutation to red on the rule it refuses, with the override in both fixtures still restored so none of them can pass on a tool that does nothing.
- **`make smoke` runs the gates against the runners the RELEASE ships, in a throwaway worktree of each consumer** — it ran the working tree's `check all` IN PLACE against whatever runners each consumer's PIN had installed: the combination a consumer occupies for the minutes between a pin bump and `install-runners --force`, and never the one a release ships. v0.23.0 walked into it — `check test-shape` asks its roster through `make integration-list`, nullbound had opted into the header rule, its installed `Makefile.devkit` predated the target, and the fix was to advance the fixture by hand ahead of the pin. `check all` and the two censuses now run inside a detached `git worktree` of the consumer with `install-runners --force` applied to it, and one row says how many files the working tree was ahead by — cross-checked against a census computed from the installable bodies and the destination modes, asked of the worktree the install actually faced, never read off the install's own report. The read-only verbs (`scene`, `refs`, `pm status`, `autoloads`) stay on the main checkout, which is what a consumer's pin runs today. A FAILING install is a red row naming the destination and `check all` does not run at all: a fallback to the in-place run would recreate the exact blind spot this closes, and it would look green. The removal is a `finally` and its outcome is its own row — `worktree_verdict` compares the PATH column, so our worktree still listed is `LEAKED` naming the removal that failed (never retried), any other path appearing or leaving is still a finding, and a list whose text moved because somebody committed under the run is reported rather than dropped; `checkout unchanged` prints the symmetric difference for the same reason (a peer agent committing in nullbound mid-run had made it say `DIRTIED the tree: []`). Live: `runners ahead  2 file(s) ahead of the consumer's install: tools/dev/runners/scenario.sh, tools/dev/runners/integration.sh` and `check all  exit 0 against the release's runners` on both consumers, 42 checks green in 57 s, and both checkouts proven untouched by sha256 over every file (13,230 on trail, 5,693 on nullbound — 2,800 and 4,839 of them tracked, 0 edited, 0 added, 0 removed; `git status --porcelain` and `git worktree list` byte-identical before and after, the only movement being gitignored `.scenario-reports/` and `.headless-userdata/` written by other agents' own gates). Thirteen tests drive it against a SCRATCH git repo standing in for a consumer, so it holds on CI where no consumer is checked out: v0.23.0's state reproduced (in place `exit 2` naming `integration-list`, through the worktree `exit 0`), a current consumer at 0 ahead, an uncommitted runner edit that is not a census disagreement, m1's header-edited runner NOT a refusal (`--force`, or every consumer that edited a header is a red smoke), a destination the installer cannot write as a red row with no fallback, and the five `git worktree list` shapes the row has to tell apart. 1641 -> 1683 tests.

- **The five unit tests that read a live consumer checkout are `make smoke` rows** — `test_defaults.ConsumerCorpus`, `test_check_props.NoFalsePositivesOnRealRepos` and the consumer-walking cases in `test_canonicalize`, `test_tscn_roundtrip` and `test_uid_codec` each walked `~/workspace` behind a `skipUnless`: on CI no consumer exists so they proved nothing, and locally four interpreters re-read two game repos per `make matrix`. `consumer_smoke.py` already says THE CONSUMERS ARE THE FIXTURES, so that is where they live now — five rows per consumer, run once, each carrying the assertion it made: the pinned `check props` finding count (ceiling 30, exit code consistent with it), parse->serialise byte-identical over every `.tscn`/`.tres` (644 and 758, floor 100), the uid differential against an INDEPENDENT positional formulation (1347 and 1823 uids, floor 200), a degraded real scene restored byte-for-byte with no header uid invented, and `--elide-defaults` deletion-only and idempotent over a real corpus. The scene for the canonicalize row is chosen by a rule — the one the degradation costs most lines — decided before any restoration is attempted, and on nullbound it reselects the `quarantine.tscn` the unit test named. The two WRITE verbs run over a `tempfile` copy and the trailing `checkout unchanged` row still passes; both checkouts were proven byte-identical across a run by hashing all 9,139 `.tscn`/`.tres`/`.gd`/`.uid`/`.import`/`project.godot` files, which git porcelain cannot see. `tests/support.available_consumers` and `CONSUMER_REPOS` are deleted with their last reader; `grep -rn available_consumers tests/` is empty. The suite drops 9.11 s on the floor interpreter and reads nothing outside this repo — 1645 -> 1641 tests, and the one remaining skip is a case-insensitive filesystem, not a missing consumer. The `shell` census is unchanged at 35/8 (nothing changed sides: `test_canonicalize` and `test_defaults` keep `temp_repo`, `test_check_props` imports `subprocess`, and the other two were already unmarked). One case is ADDED: the smoke restates the canonicality predicate rather than importing it, so `test_uid_codec` now pins the two restatements to one verdict over the corpus, the whole encoder id space, AND one spelling per CLAUSE of the predicate — measured: dropping the `'9'` rule left every corpus uid and every encoder id agreeing, because the engine emits no `z`, no `9` and no leading `a`, so a sweep over real uids alone pins the alias clauses not at all. Each of the three clauses was watched red. (tools/consumer_smoke.py, tests/support/__init__.py, tests/test_{defaults,check_props,canonicalize,tscn_roundtrip,uid_codec}.py)

- **The installed `verify.yml` brings the Godot toolchain with it** — it was this package's own CI shape (checkout, uv, `make milestone`), so a consumer's `make milestone` — which boots the engine and shells out to gdlint and shellcheck — was red on every PR for reasons that said nothing about their code, and the one consumer that worked had hand-edited its copy after install. The file now installs `chickensoft-games/setup-godot@v2`, gdlint and shellcheck ahead of the gate, each guarded by `if: hashFiles('project.godot') != ''` — so the same file is right in a game repo and in a repo with no engine in it (this package, which installs it into itself and still runs three steps). The engine's MAJOR.MINOR is READ from `project.godot` `config/features` by a step that refuses by name — no such line, no `MAJOR.MINOR` entry, a non-numeric patch — and emits no version when it refuses; the patch level is the one knob, an `env: GODOT_PATCH` at the head of the file. Consumer follow-up: `install-ci --diff` then `--force`, re-applying nothing. (repo/installables/ci-verify.yml, tests/test_ci_workflows.py)

- **The installed `verify.yml` ARMS the checkout before the gate that reads it** — `core.hooksPath` is repo-LOCAL git config: nothing tracked carries it and `actions/checkout` produces a tree that has never had it set. So a repo that runs `check hooks` in its full gate was red on EVERY clean CI run — the gate stating a true fact about the runner, `check all` exit 1, `make milestone` red, and the release protocol unable to reach its CI-green step. Reproduced on a `--local` clone of this repo at v0.24.0's HEAD: `core.hooksPath` empty, `UNARMED`, exit 1. The workflow now runs `bash tools/setup-hooks.sh` between the checkout and the gate, guarded `if: hashFiles('tools/setup-hooks.sh') != ''` exactly as the engine steps are guarded on `project.godot` — a repo that does not ship the arming script has no corpus to arm and the step skips, so the three-step run of a repo with neither file is unchanged. This is also the correction to a claim: CI running the same TARGET as the local full gate does not make the two answer the same, because a gate may read state the checkout does not carry, and every such gate needs a step ahead of it. Two cases watched red against the unfixed file, one structural (nothing arms the checkout / arming lands after the gate) and one that READS the workflow's own `run:` scripts and executes them in a scratch checkout, where `check hooks` goes 1 -> 0. (repo/installables/ci-verify.yml, CLAUDE.md, tests/test_ci_workflows.py)

- **`check hooks` counts the DIRECTORY, and a non-regular entry is a finding** — the census enumerated regular files, and `Kind.FILE` is a UNIVERSE declaration in `core.walk`: a universe reason never renders in `disclosures()`, so a directory or a broken symlink under `tools/hooks/` did not merely go unchecked, it was SUBTRACTED — the number came out smaller than the directory with no line saying why, and the verdict was PASS. Measured: a corpus of three tracked entries (a working `pre-push`, a `pre-commit` symlink whose target is gone, a `prepare-commit-msg` directory) reported `1 hook(s)` and exit 0, with two of the three guards disarmed and invisible — a gate PASSing over exactly the drift it was written to catch. Git's hook universe is every entry in the directory, so this one's is too: the walk is `Kind.ANY` and a non-regular entry is `NOT A FILE`, naming what it is (`is a directory` / `is a symlink to X, which does not resolve`) and the `_`-prefix that is how a non-hook lives there legitimately. The same corpus now reports `3 hook(s)`, two findings, exit 1. `doctor.sh` carried the identical `[ -f "$hook" ] || continue` skip and reported `ok tracked hook pre-push present + executable` and nothing else — fixed in the same shape, with the unmatched-glob case (an empty corpus) still a `warn`, not a finding, because that literal is the one thing the `-f` was load-bearing for. Two shipped surfaces agreeing on the wrong answer is worse than one. Six cases watched red against the unfixed pair, including the universal they are instances of — the number in the verdict equals the entries git would try, counted by a raw listing so the test cannot inherit the defect. (repo/checks/hooks.py, repo/installables/doctor.sh, tools/dev/checks/doctor.sh, README.md, tests/test_check_hooks.py, tests/test_hooks_payloads.py)

- **A collision withholds ITS file, so a new hook reaches a consumer whose headers are edited** — both consumers adopting v0.23.0 had four hooks differing ONLY inside the `project config` header the file invites them to edit, and one collision refused the whole roster: the two ledger couriers that release ADDED — pure additions, nothing in their way — could not be installed at all, and the way through was `--force` plus four re-edits or hand-copying files out of a uv cache. The whole-plan decision stands; what a collision withholds is that destination. The entries with nothing in their way are written, every collision is named, and the run still exits **1**, because a caller reading the exit code alone must not be told the roster is on disk when one of it is the operator's own file. A difference confined to the config block is now REPORTED as one — in the refusal (`(project-config header only)` per file) and ahead of `--diff`'s hunks — which is the actionable half: the rest of that file is byte-current, so the repair is to do nothing rather than to force and re-edit. The installer does NOT merge the block, and the refusal says so: `--force` replaces the whole file, header included. Preserving a consumer's header under a newer body was measured on this package's own history — `cc-godot-sandbox.sh`'s header gained `SANDBOX_FUNCTION` in 0.16.0 and `GDK_BOOT_FUNCTIONS` in 0.19.0, and grafting the 0.16.0 header onto the current body drops four keys the body reads under `set -u`: the hook dies on `GDK_BOOT_FUNCTIONS: unbound variable` before it decides anything and exits 1, where only exit 2 is a BLOCK — so a raw `godot --headless` goes straight through a guard that is on disk, looks installed, and stops nothing. A DEFECT (a directory, a read-only path) still refuses the whole command with nothing written — it is not a decision the operator made about that file. 33 tests, each watched red against the previous commit. (repo/install.py, README.md)

- **`make matrix` runs bash once** — the suite is four interpreters in series and ~85% of its wall clock is `subprocess` (bash, make, git, the installed hook corpora), none of which a Python version changes. `PY_FLOOR` now runs the whole suite and every other interpreter in `PY_MATRIX` runs `-m "not shell"`; the `shell` mark is DERIVED per module at collection in `tests/conftest.py` from what the source imports, so no list here can go stale and a hand-applied mark is a collection refusal. The verdict line is byte-identical (`[MATRIX] PASS on 3.11 3.12 3.13 3.14`), `make test` still runs everything, and `verify.yml` needed no edit — this package's own CI step is the visible difference. A `PY_FLOOR` that is not in `PY_MATRIX` is refused by name before the first interpreter starts, exit 2, nothing spawned: a matrix with no full pass would print PASS over a suite that never ran. (Makefile, tests/conftest.py, tests/test_makefile_gates.py)

- **The ledger couriers carry every payload value in the environment, so the vehicle's shell cannot eat one** — `mk_arg` spelled each value into `ARGS=` with `printf %q`, which is BASH's quoting: `Makefile.devkit` sets `SHELL := bash` so the stock route decoded it, but under a hand-rolled Makefile whose recipe shell is dash a non-ASCII transcript path arrived as the literal `$'/…/caf\303\251/t.jsonl'`, `pm ledger record` refused "is not a file" at exit 2, and the hook exited 0 — the row was lost with nothing red anywhere and the milestone read as cheaper than it was. It is locale-sensitive on top of shell-sensitive: `printf %q` leaves the same path bare under a UTF-8 locale and escapes it under `C`/`POSIX`/unset, so the identical courier, path and vehicle record perfectly on the machine that happens to have a locale and lose the row on the one that does not — which is why it survived a corpus, a suite and a release. `env_arg <flag> <name> <value>` replaces it: the value is exported and the flag appended in ONE call, so neither can be written without the other, and `ARGS` carries a FIXED reference (`"$$GDK_LEDGER_TRANSCRIPT"`) — the same bytes for every value, so no property of a path can change how the vehicle parses the word. make hands its environment to a recipe untouched; the reference is POSIX and no shell re-scans the result of a parameter expansion, so the value reaches the verb byte-exact whatever is in it. Both couriers' `--self-test` corpora gain a dash vehicle with a non-ASCII, spaced path under a pinned `LC_ALL=C`, watched red against the old transport (`ARG[$/…/caf\303\251 dir/t.jsonl]`); the suite adds the same vehicle over seven path shapes — non-ASCII, space, apostrophe, `$`, backtick, semicolon and a NEWLINE, which no spelling into a make command-line variable survives at all — plus a case asserting the mechanism rather than the outcome: the path must not appear in `ARGS` at all. 16 cases across both couriers, 6 of them red at `aaec4bd`. The consumer-visible requirement is one line in each config header: the vehicle must pass its environment through. (repo/installables/cc-ledger-{session,subagent}.sh, tools/hooks/, tests/test_hooks_payloads.py)

- **The scenario runner's cache recovery escalates, and `doctor` says a tree is unrepairable BEFORE the sweep** — a consumer ran 147 scenarios against a `.godot` whose uid index had lost 56 tracked sidecars: every scenario booted, printed its own `[SCENARIO] <name> PASS steps=N errors=0`, and was FAILED by the runner's `invalid UID … using text path instead` sweep. 147 of 147, green inside. The recovery detected the class precisely — uid class AND the scenario's own PASS AND no hang — and then applied `gdk_rebuild_import_cache`, which cannot repair it: an import pass against the EXISTING directory mints sidecars for files that are NEW and does not restore index entries already lost. Measured three times in that tree, including once after deleting `uid_cache.bin` alone: 1780 entries and the same 56 missing every time; `rm -rf .godot` then a rebuild gave 1822 in 21 s. So the runner retried the same non-remedy 147 times. `scenario.sh` now escalates — rung 1 rebuilds, and when the retry reports the SAME uid class rung 2 removes `.godot/` and rebuilds once more before a final retry. Two rungs of straight-line code, never a loop: a third failure is a real failure and stays fast, and a genuinely failing scenario (no PASS line) still costs zero remedies and one boot. Rung 2 throws away a local editor's cache state, so it says `REMOVING .godot/` before it acts and says it whether or not the repair then works; and it DECLINES inside a sweep — `integration.sh` now exports `GDK_SCENARIO_IN_SWEEP=1` to every job, because removing the one `.godot` that N parallel peers are mid-boot in would convert one cache defect into a scatter of failures that look like real ones. There the run prints `rm -rf .godot && make import-cache` and fails, so the operator repairs once, serially. The condition itself is now `cold_cache_only <report> <exit>`, pure over its arguments and in the `--self-test` corpus (16 → 20 cases; dropping the PASS conjunct is caught by name). `doctor` gains the cheaper half: it counts the tracked `.uid` sidecars against the entries in `.godot/uid_cache.bin` and FAILS with `rm -rf .godot && make import-cache` and the first five missing paths — one red line before a 147-scenario sweep instead of 147 reports after it. Membership is decided by extracting the index's path tokens with `tr` and matching by PREFIX in awk, NOT `grep -a -o -F -f` over the binary: BSD grep and ugrep 7.8.4 disagreed by 75 paths on the same 110 KB cache, which would have made the verdict depend on which grep the host installed (it reported trail as 26-short when it is not). A `.gdignore`d directory is excluded by prefix (nullbound has three); a path that is a proper prefix of another tracked path cannot be decided by a prefix match at all and is reported as unverifiable and left OUT of the census rather than counted present — zero such pairs in either consumer. Live: nullbound `uid index covers 1214 tracked .uid sidecar(s)` in 1.1 s, trail 764, this repo silent (no `project.godot`). 14 tests: **11 watched red at `aaec4bd`** (the whole escalation, the bound, the sweep guard, the fan-out marker with the file reverted to HEAD and back — 3 -> 0 -> 3 occurrences — and six doctor cases), **2 controls green before AND after** (a genuinely failing scenario is never retried; a repo with no `project.godot` says nothing about uids), and **1 proven by mutation** — swapping the awk prefix match for equality reds exactly the case about a path the next entry's id glued eight printable bytes onto, and nothing else. (repo/installables/{scenario,integration,doctor}.sh, tools/dev/checks/doctor.sh, tests/test_{runners_installable,hooks_payloads}.py)

- **`make hooks` arms this checkout and `check hooks` reds an unarmed one** — `CLAUDE.md` said the hook corpus was self-hosted here while `core.hooksPath` was UNSET in every checkout of this repo, for two releases: the guards were on disk, tracked, executable and reviewed, and git consulted none of them. Consumers get that question answered by `make doctor` out of `Makefile.devkit`; this repo has its own Makefile and never grew the equivalent, so nothing in `make precommit` looked. `make hooks` (`bash tools/setup-hooks.sh`) is the repair; `check hooks` is the report, and it is in this repo's `[checks] all`, so it runs inside `make gates` and therefore inside `precommit` and `milestone`. It asks three things, and the third is the one a path check gets wrong: `core.hooksPath` resolves to `tools/hooks` (UNARMED when unset, MISDIRECTED when it points elsewhere, naming both paths), every entry carries an exec bit (git skips one that does not, in SILENCE), and every hook still RUNS — a `cc-*.sh` fed a payload it cannot read must fail OPEN at exit 0, which executes the whole file including the project-config header a consumer edits, and anything else in the directory must parse. Armed is not working: grafting `cc-godot-sandbox.sh`'s 0.16.0 header onto its current body drops four keys the body reads under `set -u`, so the hook dies on `unbound variable` and exits 1 where only 2 is a BLOCK — installed, executable, and stopping nothing. That state is now a DEAD finding carrying git's own error. The probe is derived from each hook's SHAPE, never a roster, so the next hook to ship is covered without this gate being edited; `_*` libraries and `*.local` drop-ins are excluded through `core.walk`, so the two are DISCLOSED in the census rather than subtracted from it, and an empty corpus is a FAIL and never a PASS over nothing. Stock OFF for consumers — arming is a decision each makes once, and a gate that reddened them on upgrade day would be the roster being wrong for the repo. Not a second hook suite: behaviour stays with `tests/test_hooks_payloads.py` and `make hooks-self-test`, which all run a COPY in a temp repo; this asks the one question none of them can. 11 cases, and the four states watched at `aaec4bd`: an unarmed clone there passes `make gates` and has no `hooks` target at all. (repo/checks/hooks.py, cli.py, devkit.toml, Makefile, README.md, tests/test_check_hooks.py)

- **`scene canonicalize` no longer invents an `[editable path=]` the source never carried** — it added one to **26** instance hosts across 10 tracked consumer scenes (25 nullbound, 1 trail), e.g. `systems/processes/process_widget.tscn` gaining `[editable path="Layout/Header"]`. The rule was *"an instance whose children are overridden is an editable instance"* — false, and nothing in a packed scene is evidence for it. `[editable]` records ONE thing, the editor's per-instance **Editable Children** toggle: the engine emits it only from the live `is_editable_instance(node)` flag and applies it LAST on load, after every node and property already exists, by calling `set_editable_instance()`; a type-less override entry is emitted when the node's `states_stack` is non-empty **or** it is part of an editable instance — two independent routes, so instance-child overrides are written with no marker involved (`scene/resources/packed_scene.cpp`). Both trees say the same thing in BOTH directions, which no derivation rule can produce: 26 overridden hosts with no marker, and **14** markers on hosts with no overridden child at all. It is authored state, not a `pack()` loss — `pack()` preserves it whenever the flag was set, and an offline authoring script that never set it has nothing to restore. `_restore_editable_markers` is deleted with its constant; the verb now neither writes nor removes an `[editable]` section, and the module docstring says why (it had enumerated three losses and never listed this one, while the code contradicted its own *"restored from evidence, never invented"*). Measured over the live trees: 26 invented before, **0** after, **0** `[editable]` lines removed. Three tests, all three watched red at `52c25b4`: a sweep of all 36 committed corpus scenes asserting the `[editable]` list is byte-identical before and after (floors of 7 override-carrying scenes and 21 declared markers, so a corpus that rots fails here instead of proving less), the `canon_repo` fixture gaining none, and the OPPOSITE failure — a scene that DOES declare Editable Children, with a ref uid and an `index=` degraded away, coming back with both restored and its marker untouched. **The smoke's `canonicalize round trip` row can now widen to every tracked scene on nullbound (194/194); on trail 13 of 116 still fail, for DIFFERENT defects** — none of them the marker, and the shape of them is measured in the next bullet rather than guessed at here. (godot/write/scene_canonicalize.py, README.md, tests/test_canonicalize.py)

- **`scene canonicalize` reaches the ROOT when it looks for the instance an override belongs to — and still refuses to invent the `index=` a scene's own created nodes carry** — `_instance_host` walked a node's ancestors down to depth 1 and stopped, so it never considered the root. An INHERITED scene's root IS an instancing ancestor (`[node name="X" instance=ExtResource(base)]`, its children the base's children), so every override directly under one was unresolvable: it lost its `index=` and got `no instancing ancestor was found` instead of the ordinal the base plainly gives it. Live on trail: 3 overrides across `scenes/moments/force_resolution.tscn` and `scenes/moments/game_over.tscn`, both of which now round-trip; the tree's unresolved index losses go **3 -> 0** and nullbound stays at 39 of 39 restored, 0 invented. **The filed diagnosis's other half does not survive its own corpus, and is NOT implemented.** It read *Godot writes `index=` on newly added `type=`d nodes in an inherited scene, and `_restore_indexes` skips anything carrying a type* — but a node the scene CREATES (`type=` builds it, `instance=` instantiates it) is placed by no base, so no ordinal exists to count. Over both trees, 12 created nodes carry an authored `index=` and no rule reproduces them: 3 of the 12 name a slot no count reaches (`dossier`, `settlement_menu`, `settlement_menu_compound` say `index="2"` where their base gives the root 3 children), while 4 structurally identical siblings — an added node under an inherited root, same repo, same week — carry none. It is authored state, like the `[editable]` marker of the bullet above, and restoring it would be the same defect in the same module twice; a probe that does restore it invents **505** `index=` attributes across the two trees (319 trail, 186 nullbound). So **the round-trip smoke row is NOT widened**: it can widen on nullbound (194/194) and cannot on trail, where 11 of 116 scenes still fail — 10 for the authored-index class above and 1 for a value that names an impossible slot (`rest_moment.tscn` says `index="3"` for `Content` under a base `Inner` that has had exactly 3 children, Paper/Border/Content, in every commit of its history, and the scene adds none, so the only in-range answer is the counter's 2). An asymmetric row or a pinned known-fail list would be the widening in name only, so the census is reported instead. Five tests: **3 red against `99cce63`** (an override under an inherited root, an override of a base GRANDchild counted through the root, and the whole inherited scene round-tripping byte-for-byte), **2 controls green both sides** (a created node gains no index; an override the base does not place is refused by name, not guessed) — and the first control is proven by mutation: an over-correction that falls back to the next free slot reds it with `index="0"` on the `type=`d node and `index="2"` on the instanced one. (godot/write/scene_canonicalize.py, tests/test_canonicalize.py)

## v0.23.0 — 2026-09-03

**Telemetry.** The devkit records what a milestone cost, at the moment of the work, and never judges it: every status transition and decision is a timestamped row in the milestone's own `ledger.jsonl`, two installed hooks carry each Claude Code stop's transcript to `pm ledger record`, review records end in a machine-readable verdict block, bugs can name the feature that caused them, and `pm ledger report` adds the raw rows up into the five questions without a weight, a price or a label. Decisions D1–D11 in `pm/roadmap/0.23.0-telemetry/decisions.md`; D11 folds the 0.22.1 diff-slice-hardening milestone — PR #3, the three runner/gate bullets at the end of this section — into this release, so no v0.22.1 ships.

- **Every status flip and `pm decide` write themselves into the milestone's ledger** — `pm story|feature|milestone|bug <status>` and `pm decide <id> <title…>` each append one JSON row (`ts` full UTC to the second, `kind`, the grain, and the transition or the decision) to `pm/roadmap/<milestone>/ledger.jsonl`, and only after their own frontmatter write has landed — never before, so a refused flip writes nothing; a no-op flip still counts, and `--cascade` appends one row per story it closes. `pm init` now appends `pm/roadmap/*/ledger.jsonl merge=union` to `.gitattributes`, so two milestone branches' rows merge as a union instead of a conflict. Existing output lines and exit codes are unchanged; a row that cannot be written prints a warning on stderr rather than turning a landed write into a failure. (repo/pm/ledger.py, repo/pm/cli.py, repo/pm/skills.py)
- **`pm ledger record` sums a Claude Code transcript into one row; `pm ledger show` prints a grain's timeline** — `pm ledger record --from-transcript <path> --event SubagentStop|Stop [--session-id …] [--agent-id …] [--agent-type …]` sums `message.usage` (tokens in/out/cache-create/cache-read) over the transcript's assistant records, counts `tool_use` blocks by name and the calls before the first `Edit`/`Write`/`MultiEdit`/`NotebookEdit`, and appends a `dispatch` (SubagentStop) or `session` (Stop) row — carrying the tree's live state at that instant — to the building milestone's `ledger.jsonl`; `--grain <id>` files a row by hand instead. A field the transcript lacks is an absent key, never a zero; a transcript with no assistant record, a non-JSON line, a bad timestamp or number, an unknown `--event`, or no building milestone (or two of them) refuses at exit 2 without writing. `pm ledger show <grain-id> [--json]` prints that grain's rows oldest first, the seconds since the previous status row, and a total once the last row enters the terminal state of its vocabulary (`done`, or `bug_states[-1]` for a bug). (repo/pm/ledger.py, repo/pm/cli.py)
- **`pm ledger report` answers five questions from raw rows, per story/feature/bug** — spend (dispatches, the four token sums, tool calls and wall-clock, split by agent type and by state); yield per review pass (findings by severity and by disposition, read from the verdict block below); rework (review→wip reopens, dispatches after a story first reaches review, the verdict spread); escapes (bugs whose `caused_by:` names a feature, with the bug's and the feature's status); and overhead shape (tool calls before a dispatch's first write, decisions per grain and the seconds to the next status row after one, session-to-session deltas). Arithmetic only — sum, count, subtract, group, never a weight or a score — over the building milestone by default or one named explicitly; a milestone with no `ledger.jsonl` prints its `no ledger` line and exits 0 — and still prints the sections that never read the ledger (yield from the review records, escapes from `caused_by:`) when there is something in them; no number in it ever exits non-zero. `--json` prints the same data as one object. (repo/pm/report.py, repo/pm/cli.py)
- **Review records end in one machine-readable verdict block, read by a new parser** — `verdict: SHIP|SHIP-WITH-FIXES|HOLD|RELEASE-SAFE|RELEASE-WITH-FIXES|NOT-RELEASE-SAFE` followed by a `| id | severity | disposition |` table, one row per finding, across twelve severities and a disposition of `landed <hash>`, `landed in-place`, `rejected: <why>` or `deferred: <grain-id>`. `repo/pm/verdict.py` detects anything shaped like a block and then accepts nothing partway: an unknown verdict or severity, a malformed row, a second block, an unfenced near-miss or a fence that never closes each refuse by line number; a record with no block at all is not an error — it is `NoVerdict`, which `pm ledger report`'s yield section lists by name rather than counting as zero. The four reviewer-shaped definitions (`reviewer`, `simplifier`, `milestone-reviewer`, `verification-reviewer`) each carry the instruction to emit it. (repo/pm/verdict.py, repo/installables/{reviewer,simplifier,milestone-reviewer,verification-reviewer}.md)
- **`pm new bug` gains `--caused-by <feature-id>`** — stamps the new optional `caused_by:` field, the feature whose change produced the bug at any status, as distinct from `caught_in:`, the milestone that found it; an id that does not resolve to a feature refuses at exit 2 and writes nothing. `pm validate` and `check pm` resolve it under V4 alongside `depends_on`/`consumed_by`; a bug is not counted as a grain in the ref census. (repo/pm/cli.py, repo/pm/validate.py, repo/checks/pm.py, repo/pm/templates/bug.md)
- **`install-hooks` gains two ledger couriers, and prints the settings that arm them** — `cc-ledger-subagent.sh` (`SubagentStop`) and `cc-ledger-session.sh` (`Stop`) each read the event JSON with one `json.load`, expand a leading `~`, and hand the transcript path plus whatever ids the payload carries to `pm ledger record` through `make pm ARGS="…"`; every path out is exit 0, the reason on stderr, and a vehicle that exits 0 without a word is called out by name rather than mistaken for a written row. `install-hooks` now prints the `.claude/settings.json` entries that fire the corpus — the two couriers `"async": true`, `cc-ledger-subagent.sh` with no matcher — printed, never written. (repo/installables/cc-ledger-subagent.sh, repo/installables/cc-ledger-session.sh, repo/install.py)
- **This repo self-hosts the hook corpus and the ledger couriers** — `install-hooks` run against the devkit itself: `tools/hooks/*`, `tools/setup-hooks.sh`, `tools/dev/agent-worktree.sh`, `tools/dev/checks/doctor.sh`, and a `.claude/settings.json` carrying the entries the verb prints (the two couriers `"async": true`, reaching `pm ledger record` through `make pm`); the `project config` headers say this repo's gates (`make gates`, base `main`); `make hooks-self-test` replays the three shipped corpora and joins `precommit` and `milestone`; `check shell` joins `[checks] all` now that the tree holds shell under `tools/`. Proven: each courier exits 0 on a SubagentStop/Stop payload naming a fixture transcript — fail-open with the verb's refusal on stderr while no milestone is building, one row appended to `pm/roadmap/0.23.0-telemetry/ledger.jsonl` on a copy with 0.23.0 flipped to `building`. (Makefile, devkit.toml, .claude/settings.json, tools/)
- **This repo's own `make pm` runs the tracker from source, `.PHONY`** — `pm: PYTHONPATH=src python3 -m godot_devkit.cli pm $(ARGS)`, the vehicle the two ledger couriers above call when they fire on this tree; an un-`.PHONY:`'d `pm` target is exactly the silent vehicle they warn about. (Makefile)
- **`tests/test_boundaries.py` read the `open()` mode from the wrong argument for the `Path.open` spelling** — the builtin's mode is its second positional argument, `p.open(...)`'s is its first, and reading index 1 for both let every `p.open('w')`/`p.open('a')` under `src/` pass the one-writer boundary unseen. The mode slot is now read by the spelling that is calling; `repo/pm/ledger.py`'s append is the one sanctioned exception, pinned by path and by mode. (tests/test_boundaries.py)
- **`pm ledger report <milestone-id> --from <rev>` reads a retired milestone out of git** — its `ledger.jsonl`, milestone document, features, stories, bugs and review records at `<rev>` (`git show`, one blob per file) instead of off disk. Same table, same numbers, same `--json` object; the section headings gain ` — at <rev>` and `--json` gains a `rev` key. Never writes, never checks anything out; refuses at exit 2 on a rev that does not resolve (carrying git's own message), a milestone or required path absent at that rev, `--from` without a milestone id or without a value, a rev starting with `-` or holding whitespace, and `git` not on PATH. (repo/pm/report.py)
- **`integration.sh --diff <ref>` compares the touched list REPO_ROOT-relative and literal** — `git diff --name-only` names a path relative to the git TOPLEVEL and C-quotes a non-ASCII one under `core.quotePath` (git's default), while a `## covers:` entry is repo-relative and literal: a Godot project below the toplevel (`game/` in a monorepo) compared `game/systems/alpha/x.gd` against `systems/alpha`, a `café.gd` arrived as `"caf\303\251.gd"`, and either way the slice was smoke alone with no hint. Now `--relative` from the project root under `core.quotePath=false`, for the diff and the untracked list alike; a change outside the project is not a touched path of it, and the tier's own ground is ground below the toplevel too. Self-test 85 → 87 cases; three end-to-end cases against a monorepo fixture and a non-ASCII path, each watched red first. (repo/installables/integration.sh)
- **a `## covers:` entry with a doubled slash is refused by name, in the gate and the runner alike** — `check test-shape` normalised with `rstrip('/')` and `Path()`, so `systems/alpha//` and `systems//alpha` both read as a directory that exists and PASSED, while `integration.sh` drops ONE trailing slash and compares strings, so the same entry was a declared prefix of nothing: the scenario counted as covered and `--diff` never selected it, and nothing said so. One grammar now — exactly one trailing slash is spelling, and a slash with nothing after it is an empty segment, refused by that name on both sides; in the runner the scenario reads as UNDECLARED and is reported instead of silently never matching. Two matrix rows and one e2e case for the gate and the runner, a corpus case for the reader, each red first. (godot/checks/test_shape.py, repo/installables/integration.sh)
- **`check test-shape` asks the header rule of the roster the runner boots — `integration.sh --list`, new** — the gate scanned `git ls-files` under the tier minus the infra basenames (160 on one consumer) while the runner discovers with `find` minus `support/`, the `_capture` tools and its keep-list (137): the opt-in header rule demanded `## Boots because:` / `## covers:` of 22 capture tools and a support stub `--diff` can never slice to. One roster now, and the runner owns it — discovery's rule lives in the runner file's config block and in `GDK_*` env, both the consumer's to edit and neither readable by a TOML gate, so a census re-derived in the gate was a second answer by construction. `--list` prints the roster (one repo-relative path per line, sorted, booting nothing; an EMPTY roster is exit 1), and the gate reads it from `[test_shape] runner` (stock `tools/dev/runners/integration.sh`, where `install-runners` writes it; absolute, dotted, doubled-slash and backslash values refused). `header = true` with no runner there is exit 2 naming the path; a runner installed before `--list` existed is exit 2 naming `install-runners --force`; a runner that boots nothing is a FAIL, never a PASS over nothing; a keep-listed capture is on the roster and IS asked. A `header_ledger` line naming a file the runner does not boot is STALE — **at the pin bump, `install-runners --force` and drop those lines** (on nullbound: 23 lines — the 22 capture tools' and the support stub's — when its Makefile's keep-list does not reach the runner, 13 when it does, the 10 keep-listed captures being on the roster). The cap and the tier balance keep the tracked census: a capture tool that grew past the cap is tier weight whether or not the sweep boots it. 8 gate cases (one a 6-row refusal matrix), 4 e2e cases, 3 corpus cases (91 → 94), each red first. (godot/checks/test_shape.py, repo/installables/integration.sh, repo/installables/project-devkit.toml)
- **`check test-shape` asks the roster THROUGH `make integration-list`, so the census is the tree's and not the caller's** — `runner_roster` spawned `bash <runner> --list` with the gate's own environment, and a consumer's keep-list is a Makefile export (`GDK_CAPTURE_GATE_RE := …` + `export`): under `make check` the roster was 147, from the `tools/dev/devkit` shim or a bare `uvx` it was 137 — 13 STALE ledger lines one way, 23 the other; never a silent PASS, but a census that was a function of who ran the gate (rule 5). The gate now runs `make -s integration-list GDK_RUNNERS_DIR=<directory of [test_shape] runner>` — a new `Makefile.devkit` target, `bash $(GDK_RUNNERS_DIR)/integration.sh --list` and nothing else — with every `GDK_*` and `MAKEFLAGS`/`MFLAGS`/`MAKELEVEL` of its own environment dropped, so the roster is what a clean-shell `make integration` would boot: measured on a copy of nullbound, 147 and 13 STALE from a plain shell AND under make. A Makefile without the target is exit 2 naming `integration-list` (`include Makefile.devkit` defines it; `install-runners --force` writes the current one); the runner's own exit comes back through make's `Error N` line, so a runner that boots nothing is still a FAIL and a pre-`--list` runner still exit 2 naming `--force`. Why the target and not a `devkit.toml` key: the runner is shell and reads no TOML, so a keep-list in the config would have been a second copy of the Makefile's — the same drift with the sign flipped. Three gate cases red first (a Makefile export reaches the roster; a keep-list in the gate's own environment does not; a Makefile without the target names it). (godot/checks/test_shape.py, repo/installables/Makefile.devkit)
- **`check test-shape` FAIL — one census per line** — the summary read `N finding(s) across 160 scenario(s)` while the roster line said 147: the cap census and the roster in one sentence, and neither number was the other's. With `header = true` the verdict line now carries the count alone, followed by `size: X finding(s) across S scenario(s) under tests/integration/` and `header: Y finding(s) across R the runner would boot`; with the header rule off the line is unchanged. (godot/checks/test_shape.py)
- **`check test-shape` output, the lines v0.22.0's notes left out** — under `header = true` the PASS verdict ends `(N the runner would boot)`; an info line `roster: N scenario(s) the runner would boot (<runner> --list) — the header rule is asked of exactly those` prints beside the tier balance and the two ledger counts; a roster entry the gate cannot open is the finding `UNREADABLE  <path> — on the roster <runner> prints, not readable on disk`; `STALE` reads `on [test_shape] header_ledger but not a scenario the runner boots (a capture tool, a support stub, infra, or gone); drop the line`. `init`'s seeded `devkit.toml` carries six more commented lines under `# [test_shape]` — `header`, `header_ledger`, `runner`, and the three lines saying why. (godot/checks/test_shape.py, repo/installables/project-devkit.toml)
- **`check test-shape` refuses a roster of nothing from a green `integration-list`** — a target that exited 0 having printed nothing (one that never asked the runner) returned `[]` from `runner_roster`, and the gate read `roster: 0 scenario(s)` then PASS `(0 the runner would boot)`: a PASS over nothing, the one thing the module says it never does. Now exit 2 by construction in `runner_roster` — `` `make integration-list` exited 0 and printed NOTHING `` — beside the runner's own `boots NOTHING` FAIL (exit 1), which stays the answer when the runner was asked and had nothing. (godot/checks/test_shape.py)
- **A fourth disposition, `open` — raised, not yet acted on** — the block's grammar had `landed <sha>` / `landed in-place` / `rejected: <why>` / `deferred: <grain-id>` and no honest kind for a finding nobody has landed yet, so a review record written before the landing pass (every release review is) misfiled its findings as `rejected:` or exited 2 on arrival. `open` (a bare token, optionally `open: <note>`, never free text) parses as its own kind with the note carried raw; `pm ledger report`'s yield section counts it in its own `open` column beside the three — the column set is arithmetic over `DISPOSITION_KINDS`, nothing reweighted — and the refusal for an unknown kind names all four forms. The paragraph and example block every reviewer-shaped definition carries say so, still one paragraph across the four installables and the repo-local `code-reviewer.md`. (repo/pm/verdict.py, repo/pm/report.py, repo/installables/{reviewer,simplifier,milestone-reviewer,verification-reviewer}.md)

## v0.22.0 — 2026-09-03

**A scenario declares its coverage.** Three consumer bugs in the integration runner's one matcher and one in the warnings gate's reach, fixed at the source.

- **`integration.sh --system <dir>` selects the DIRECTORY, and an empty slice is a FAIL** — the flag matched a scenario NAME prefix: `--system threads` found nothing in `tests/integration/threads/` (the files are `thread_*.gd`) and two agents in a row concluded the system had no coverage, while a typo'd prefix quietly under-selected and `--system ..` selected everything. Now `<dir>` is one directory name under the source dir (letters, digits, `_`, `-`; a separator, a dot segment, a glob, whitespace, a leading dash or an over-long argument is exit 2 before any lookup), a directory that does not exist is exit 2 naming the ones that do, and a directory holding no gate is exit 1 saying what it holds — never `no scenarios matched` read as "no coverage". (repo/installables/integration.sh)
- **`integration.sh --diff <ref>` — the per-change slice, from what the scenario declares** — a scenario's header (the leading comment block) carries `## covers: <repo-relative prefix>[, …]`; the runner maps the paths the working tree changed against `<ref>` (staged or not, plus untracked) to every scenario whose entry is a component-boundary prefix of one (`systems/alpha` covers `systems/alpha/x.gd`, not `systems/alphabet/x.gd`), plus every touched scenario, plus `GDK_SMOKE_SCENARIO`. A change to the tier's own ground (`GDK_SCENARIO_SUBSTRATE_RE` — fixtures, the runners — or a basename in `GDK_INTEGRATION_INFRA_RE`) makes every scenario the honest slice and says so. A scenario declaring nothing is REPORTED by name and count in the summary line, never silently skipped; it rides only `--all`, which stays the milestone gate. A hostile entry (absolute, `..`, `res://`, a glob, whitespace, a backslash) is dropped by the reader and can select nothing. Self-test 12 → 85 cases; 16 end-to-end cases against a git fixture and a stand-in runner. (repo/installables/integration.sh)
- **`Makefile.devkit` — `integration-diff` (`REF`, default `HEAD`), and `precommit` runs it instead of `smoke`** — the slice is smoke plus whatever the change's declared coverage selects, so a clean tree runs exactly what `smoke` ran. 27 standard targets. (repo/installables/Makefile.devkit)
- **`check test-shape` — a scenario says why it boots and what it covers (opt-in `[test_shape] header = true`)** — the line cap catches a unit suite wearing a scenario's clothes after the fact; nothing asked at authoring time whether the scenario needed to exist. CHECK 3: every scenario off `[test_shape] header_ledger` carries `## Boots because: …` naming a `tests/` path (the unit test that could not assert the claim without a boot, or the scenario it extends) and `## covers: …` whose every entry is a repo-relative prefix that exists in the tree — absolute, dotted, schemed, globbed, spaced, empty and over-long entries each refused by name. CHECK 4: a ledgered scenario that grew a header is a finding naming the ledger line to drop, and a ledger entry naming no scanned scenario is STALE — the ledger only shrinks. Stock off, so a consumer that has not declared the rule is byte-identical. 20 cases + a 12-row refusal matrix. (godot/checks/test_shape.py)
- **`warnings.sh` reaches every `.gd`, not only what the import pass loads** — a `class_name` script nothing instantiates (reached only by another script's `extends`) got the lightweight class-registry pass and never the reload that emits analyzer warnings; a consumer found two shadowed-global sites the editor console printed on every F5 and the gate never saw. Stage 2 runs `compile_sweep.gd` in the same promoted mirror, so every script is analyzed and the verdict reports N/N; a sweep that produced no result line is a FAIL, not a PASS over the scripts it skipped. The sweep-transcript readers move into `gdk_runners.sh` as `gdk_sweep_result_line` / `gdk_sweep_result_field` / `gdk_sweep_failed_paths`, one definition read by both `parse.sh` and `warnings.sh`; the library corpus proves them (46 → 51 cases) and `parse.sh`'s own corpus keeps its argument surface (10 → 4). `GDK_PARSE_SWEEP_SCRIPT` names the script for both. (repo/installables/{warnings.sh,parse.sh,gdk_runners.sh})

## v0.21.0 — 2026-09-03

**Release integrity.** A nullbound cloud session found three things standing between a consumer and a release it could trust: the pre-push gate was red on every Linux container for a timestamp reason, the shipped semver gate could not tell a hotfix from a mislabelled milestone, and this package's own CI had been red on its last two releases. All three are fixed at the source and proven where CI runs them.

- **`ci-semver-gate.yml` — versions of any length, and a release is a closed milestone or a hotfix** — the gate read exactly three components and dropped the rest, so a hotfix `0.90.3` -> `0.90.3.1` compared as equal and was refused, and a letter suffix (`0.90.3a`) crashed the integer test — on that PR and on every later one, because main would then carry it. Now component-wise over the longer of the two, a missing component 0, a non-numeric component an explicit refusal. And the gate now asks what the version IS, not only whether it grew: a merge to the mainline must carry the id of a `done` milestone under `PM_ROADMAP` (a step `env`, default `pm/roadmap` — set it to `[pm] roadmap_dir` if yours differs) or main's version plus one hotfix component. Nothing else — a BUILDING milestone's id in particular: on nullbound the 0.90.2 release reached main wearing `0.90.3` because the next milestone's bump-at-start landed before the close merged, Auto-Tag then named the wrong thing, and the semver gate blocked the real close. That PR is a row in the corpus and it is refused, and so is a hotfix-shaped version over a roadmap the gate could not read: an absent `PM_ROADMAP` or one with no `milestone.md` is a refusal, never an OK over nothing. 17 rows, the step's script RUN under bash against a scratch PM tree, not read. Re-install with `install-ci --force`. (repo/installables/ci-semver-gate.yml)
- **`make milestone` is green where CI runs it** — `verify.yml` had been red on the last two releases (v0.19.0, v0.20.0) for two reasons nobody could act on from the verdict line. The makefile tests spawn a `make` that inherited the parent recipe's `MAKELEVEL`/`MAKEFLAGS`, so under `make test` every sub-make announced `Entering directory` ahead of the one verdict line the tests read — green under bare pytest, red under the target CI runs; the helpers now drop the three recursion variables. And `ubuntu-latest` ships shellcheck, so `check shell` — a SKIP on a Mac without it — ran in the consumer smoke's fresh project and found six findings in three installables: an unused parameter on the hooks' `is_agent_context` (SC2120/SC2119, `pre-push`, `prepare-commit-msg`) and the self-test's indirectly-invoked helpers under shellcheck 0.9's code for the note (SC2317, `hermetic_run_scan.sh`). Fixed at the source. Proven by running the full gate under `make` with shellcheck installed, the way CI does. (tests/test_makefile_include.py, tests/test_makefile_gates.py, repo/installables/{pre-push,prepare-commit-msg,hermetic_run_scan.sh})
- **`make milestone` is green where CI runs it, second cause** — the run that carried the fix above was itself red: 9 tests, all of one shape, every one green on a Mac at the same commit. `verify.yml` exports `VERBOSE=1` for the whole `make milestone` step (CI has no second look, so it streams), the suite runs inside that step, and the cases asserting a gate is *quiet by default* spawned their `make` and `bash` with the inherited environment — so under CI every quiet run streamed, `make gates` printed seven lines where one was asserted, and `VERBOSE=1` added nothing over the quiet run because the quiet run already had it. Not a Make-4-versus-3.81 or a dash-versus-bash difference: reproduced byte-for-byte on macOS by exporting the variable, and both platforms fail the same nine. The three subprocess helpers now drop `VERBOSE` beside the three make-recursion variables they already dropped — the default the cases speak of is the variable UNSET, and a case that wants the stream sets it itself. One real library defect underneath: `gdk_runners.sh --self-test` pinned `VERBOSE` on the two cases that prove each setting and let the byte-cap case inherit it, so `make runners-self-test VERBOSE=1` streamed the cap case's eight bytes into the corpus's own verdict line (`01234567[gdk-runners] SELF-TEST OK …`). The corpus now pins `VERBOSE=0` for its whole run. Four cases added, each watched red against the unfixed helper or library: an ambient `VERBOSE=1` must not turn a quiet case loud (one per helper), and every installed runner's `--self-test` verdict is one line whatever the environment says (10 runners). (repo/installables/gdk_runners.sh, tests/test_makefile_gates.py, tests/test_makefile_include.py, tests/test_runners_installable.py)
- **`check pm` D8 — a hotfix satisfies the version check** — `<milestone id>.N` for a DONE milestone in the tree — the released one retire's lag-by-one keeps — passes D8 alongside the building milestone's own id; a `.N` on a building or planning id refuses. A hotfix (`0.90.3` -> `0.90.3.1`) is cut from the mainline and carries no milestone of its own, so D8's plain equality refused the release branch's version for no reason anyone could act on — and D8 sits inside `check all`, which sits inside the pre-push hook. `0.90.3.1a` and a `.N` on an id that is not in the tree still fail. (repo/checks/pm.py)
- **`import_cache.sh` — freshness is at-or-after the stamp, at nanosecond resolution** — `stale_cache_artifacts` compared with `find -newer`, which is strictly-newer; a Linux kernel stamps two files written in the same clock tick (~ms) with IDENTICAL nanoseconds, so a cache refreshed right behind the stamp read as stale and the runner's `--self-test` was red on every Linux container (a cloud Claude Code session, CI) while macOS's fine clock hid it — which made `make check`, and so the pre-push hook, fail on Linux for a reason nobody could act on. Now `mtime_ns` (GNU `stat -c '%.9Y'` / BSD `stat -f '%Fm'`, strictly-newer `find` only when neither answers) and `artifact >= stamp`. One corpus case added: an artifact `touch -r`'d to the stamp's exact timestamp must read fresh — deterministic on every platform, and it MISSes against the old comparison. 13 cases. (repo/installables/import_cache.sh)

## v0.20.0 — 2026-09-02

**Bootstrap.** Measured against the two consumers: the devkit gave a fresh Godot project the TOOLS (the CLI, the gates, the PM tree) and the PROCESS (hooks, agents, skills), but not the SCAFFOLDING — the Makefile, the Godot/GUT runners, the generic scans and the CI set both games actually run. Both had re-invented all four; twenty Makefile targets in each were identical one-line pass-throughs, `auto-tag` / `semver-gate` / `uid-guard` existed twice, and neither used the one workflow `install-ci` wrote. This release is that layer, and the one command that lays it down.

- **`godot-devkit init` — a blank Godot 4 project, wired in one command** — composes `pm init` and the four `install-*` verbs in the order a fresh project needs them, and writes the three files nothing else wrote: `devkit.toml` (a template carrying every `[section]` the gates read, every one commented out at its stock default, so a repo that keeps it behaves byte-identically to one with no config at all), your two-line `Makefile` with this tag substituted into the pin, and a `CLAUDE.md` skeleton naming the standard targets and the installed rules. It also appends the four run-artifact directories to `.gitignore` and RUNS `tools/setup-hooks.sh` — installing a hook is not arming it, and `core.hooksPath` skips a non-executable hook in silence. **Two ownerships, and `--force` respects the split:** the installed files are devkit-owned and `--force` overwrites them; `devkit.toml`, `Makefile`, `CLAUDE.md` and the PM tree are the project's from the first write and `--force` never touches them, so a differing seed is REPORTED rather than refused — divergence is what those files are for. Idempotent (a second run writes nothing, proven by hashing the tree before and after); `--diff` names drift on both ownerships. Two refusals decided before the first byte: a root with no `project.godot`, and a root that is not a git repo. Measured: 46 files written, `make doctor` green on the result and `make check` clean of any finding about a file the install wrote, both with zero hand edits — the three gates that read `.tscn`/`.tres` correctly report the 0-file census a project with no scene in it has, which the seed `devkit.toml` narrows in one line. (repo/init.py, repo/installables/project-{devkit.toml,Makefile,CLAUDE.md}, cli.py)
- **`Makefile.devkit` — one standard target set, and projects extend it by config** — 26 targets (`help` `doctor` · `parse` `lint` `warnings` · `unit` `integration` `integration-all` `scenario` `smoke` `capture` `import-cache` · `refs` `scene` `scene-diff` `orphans` `autoloads` `pm` · `pm-scan` `uid-scan` `hermetic-scan` `hooks-self-test` `runners-self-test` · `check` `precommit` `milestone`), devkit-owned, shipped by `install-runners` because neither half is usable alone: the runners have no callers without the targets, and every runner-backed target is dead without the runners. Your Makefile is the `DEVKIT_VERSION` pin plus `include Makefile.devkit` plus what is yours. Every gate prints ONE verdict line naming `.gate-reports/<gate>.log`, `VERBOSE=1` streams — the scenario family (`scenario` `smoke` `integration` `integration-all` `capture`) included, because none of those three runners has a streaming path a Makefile can reach and all three docs promised one anyway; the four runners that publish their own verdict are deliberately NOT wrapped again. `runners-self-test` is derived from the directory rather than a list, so a runner that lands tomorrow is proven the day it arrives — and a census of ZERO fails, because it cannot tell an uninstalled tree from a passing one. GNU make 3.81 is the floor. (repo/installables/Makefile.devkit)
- **`godot-devkit gates-extra` — your gates join `check` without forking the include** — `[gates] extra = ["my-scan", …]` in `devkit.toml`, one make target per line, read ONCE per `check` run by the CLI because make has no business parsing TOML (a `sed` over section headers is a second reader and a second answer). The value is interpolated into a make command line, so the grammar is narrow and every rejection is loud: whitespace, shell or make metacharacters, a path, an empty or over-long name, a bare string or an empty list — each exit 2 with a reason, never a silently dropped entry. Re-entry (`extra = ["check"]`, or a project gate that itself runs `make check`) is caught by a marker the sub-make carries, which holds two levels down where a list of forbidden names could not. 29-case refusal matrix. (repo/gates_extra.py, cli.py)
- **`install-ci` writes the workflow SET, not one file** — `verify.yml` (unchanged), plus `uid-guard.yml`, `semver-gate.yml` and `auto-tag.yml`: the three that existed TWICE, forked between the consumers and drifting on a project name and on which fix each fork got. Each shipped as the union of the two — trail's `contents: read` and its `${X:-0}` version defaulting, and `${{ }}` moved into `env:` so a version string out of `project.godot` can never be read as shell. The release dispatch is `RELEASE_WORKFLOW`, a variable rather than an assumption, and its absence is a green "tagged only" step instead of a red X somebody learns to ignore. Release, website and social workflows are the project's and are not written. Cross-checked: every `run: make <target>` in the set is a target `Makefile.devkit` defines. (repo/installables/ci-{uid-guard,semver-gate,auto-tag}.yml, repo/install.py)
- **seven more runners on `gdk_runners.sh`** — `parse.sh` (headless boot + a full-project `compile_sweep.gd` pass, reported N/N), `lint.sh` (gdlint over a scan set derived from git's index, never a maintained list), `warnings.sh` (the editor-only GDScript analyzer warnings, promoted to errors in a throwaway project mirror), `unit.sh` (GUT, sliced by `SYS=`, carrying whole the coverage gate that fails a run when GUT silently refused to load a test script — the disk-vs-`Totals → Scripts` count, a transcript with no totals block is exit 2, and a census of ZERO test scripts FAILS naming the directories it looked in, because `0/0` reconciles with GUT's unconditional `Scripts 0` and a typo'd `SYS=` would otherwise be a green gate), `scenario.sh`, `integration.sh` (the same scenarios, one process each, N in parallel) and `capture.sh` (HEADED, because headless is blind to render). The two that aim `rm -rf` at a configured report directory REFUSE one they may not own — absolute, `~`, or carrying a `.` / `..` segment, or holding anything git tracks — at exit 2, before a byte is read, written or removed. Every consumer-specific string is a documented `GDK_*` variable with a default — report quarantine, retention, the noise allowlist, `GDK_SMOKE_SCENARIO` — never a branch on a project name. **Every `.sh` installable is written EXECUTABLE**, by all four install verbs: the mode is part of the write in `core.apply` — the one module that mutates a filesystem here — rather than a paragraph asking the operator to `chmod +x`. It is `chmod +x` exactly, not `chmod 755`: the execute bit joins the classes that can already read the file, so a 0600 destination is not widened. A byte-current script missing the bit is REPAIRED rather than reported `already current`, because the trees this fixes are precisely the ones already installed at 0644. Self-tests: parse 10, lint 8, warnings 8, unit 15, scenario 16, integration 12, capture 10; zero `nullbound`/`trail` strings; `shellcheck -x` clean, asserted per-runner. (repo/installables/, repo/install.py, core/apply.py)
- **`hermetic_run_scan.sh` — the gate ON the runner library** — a headless run's sandbox HOME self-destructs, nothing persists beside the `runs/` spool, and no bare `trap … EXIT` clobbers the self-destruct. It ships as an installable because it can only be true of an installed PAIR (the library and the wrappers that call it), and it boots no engine, so it belongs in your STATIC gate set — `make hermetic-scan`. `--self-test` fires each check at both the hermetic shape and a planted one, each in its own scratch repo, 10 cases; five Python cases mutation-prove it goes red. Green on bash 3.2.57. (repo/installables/hermetic_run_scan.sh)
- **four project scans become check gates** — `check rng` (randomness a seeded run cannot reproduce: an UNQUALIFIED `randi()`/`randf()`/`randi_range()`/`randf_range()`, and `randomize()` in any spelling, including on an instance RNG — which makes an entropy-seeded result LOOK derived), `check tres-comment` (an authored `;` comment in a `.tres`/`.tscn`: Godot's parser accepts one and its writer DROPS it, silently and permanently), `check unit-disk` (a no-boot test reaching real persistent state — a `user://` path, a call touching a live owner, or a call whose root parameter DEFAULTS to the real one) and `check test-shape` (a ratchet on the expensive test tier). Each is config-driven, so a consumer's exceptions live in `devkit.toml` instead of an allowlist file beside a forked script — `[rng] allowlist` keys on `"<path>:<enclosing func>"` with the REASON as the value, and an entry matching no call is itself a finding. All four are OPT-IN via `[checks] all`: none can state a stock scope true of every repo, and a 0-file census correctly reddens. 41 new cases; census-exact parity with the shell scans they replace. (godot/checks/{rng,tres_comment,unit_disk,test_shape}.py)
- **`check doc` resolves the Makefile INCLUDE chain** — with the standard set shipping as an include, a consumer's Makefile is two lines, so a target roster read from the root file alone reported every `make check` in every doc as a dead target: a wall of false findings, which is how a gate gets switched off. Widening only — it can never invent a target neither file defines, and a target defined nowhere still fails. (repo/checks/doc.py)
- **`doctor.sh` has three GUT states, not two** — no `addons/gut/` at all is a fresh project that has not added a unit tier yet (warn); the directory present with `gut_cmdln.gd` missing is broken (fail, unchanged). `make doctor` was un-greenable on a just-init'd project — the one day it is most likely to be run. (repo/installables/doctor.sh)
- **the 0.19.0 release review's twelve findings, each with the test that catches it**, watched failing on the unfixed code: section-aware, order-preserving `project.godot` normalization (a same-line-set reorder made DURING the run was being reverted); `gdk_gate_capture` suspends errexit around its pipeline, so a consumer running it under `set -euo pipefail` still gets a verdict; an in-quote newline is data, not a segment break, in the sandbox hook; `find -newer` instead of bash 3.2's whole-second `[ -nt ]`; an unbounded import-cache rebuild says so rather than hanging silently; a runner root with no `project.godot` is exit 2; plus the four NITs (`gdk_pid_is_live` falling back to `ps -p` so an EPERM is not read as death, the uid-sidecar split anchored, the restore target pinned absolute at snapshot time). (repo/installables/gdk_runners.sh, import_cache.sh, cc-godot-sandbox.sh)
- **the fresh game is a gate in both tiers** — the suite builds an empty Godot 4 project, runs `init`, asserts the written file set is exactly the documented one (cross-checked against the plan tables so the literal cannot rot), that a second run writes nothing, and dry-runs all 26 standard targets off the INSTALLED include with a before/after census proving the dry runs stayed dry. The gate roster is pure parse, so `make check` is RUN rather than dry-run: no verdict may be a finding about a file the install wrote, and the gates that do apply to a blank tree (`doc`, `shell`) must PASS — the other direction, so the first assertion cannot be satisfied by a roster that scanned nothing. Dry-running it is what let `compile_sweep.gd` ship without the `.uid` sidecar `check uid` CHECK 3 asks for. `make smoke` carries the half no dry run can reach: `init` in a scratch project, the REAL `make doctor` and the REAL `make check`, splitting an init-owned failure (hooks unarmed) from a host gap (no gdlint) and saying loudly when `godot` is off PATH. Suite 1,034 tests + 3,673 subtests. (tests/test_fresh_project.py, tests/test_init_verb.py, tools/consumer_smoke.py)

## v0.19.0 — 2026-09-02

- **`install-runners` — the sandboxed headless-run shell library is an installable** — `tools/dev/gdk_runners.sh` + `tools/dev/runners/import_cache.sh`, ported from the consumer that grew them (nullbound `tools/dev/_common.sh`, 433 lines; trail's copy was the 115-line ancestor of the same four seed functions, already drifting). What a consumer gets without writing any of it: **one verdict line per gate** naming its full transcript (`[PARSE] PASS (…) — full log: .gate-reports/parse.log`), with `VERBOSE=1` restoring the stream and a 50 MB cap so a runaway boot cannot fill the disk; a **per-run self-destroying HOME sandbox** (the only redirect Godot honors on macOS) that reaps a SIGKILLed run's leftovers on the next run and never touches a concurrent run's live home; a **bounded-run contract** that tells a hang (124/137) from a failing gate; and a **`project.godot` restore** that undoes engine re-serialization and leaves a real edit alone, one line either way. The runner is the sanctioned `make import-cache` entry point: bounded (300s, `GDK_IMPORT_CACHE_TIMEOUT`), outcome-checked (the rebuild swallows the engine's exit code, so BOTH `.godot/uid_cache.bin` and `global_script_class_cache.cfg` must come out newer than a pre-pass stamp), and it reports only the paths THIS run dirtied, splitting `.uid` sidecars to commit from re-serialization to revert. Both files carry `--self-test` (29 and 10 cases). Every function is `gdk_*`: a consumer keeping its own prefix is a second name for the same fact and is not supported. (repo/installables/gdk_runners.sh, repo/installables/import_cache.sh, repo/install.py)
- **the devkit's own `make` targets are quiet** — `test`, `gates`, `fuzz`, `matrix` and `smoke` now run through the `gdk_gate_capture` / `gdk_gate_verdict` this release ships, sourced straight from `src/`: one verdict line per target naming `.gate-reports/<target>.log`, the failing lines verbatim plus the verdict on red, `VERBOSE=1` for the stream. Measured on this tree, default output: `gates` 7 -> 1, `test` 14 -> 1, `fuzz` 3 -> 1, `matrix` 69 -> 1, `smoke` 28 -> 1, `milestone` 104 -> 3 (one per member). Before it, an agent reading the full gate here had to invent a five-shape grep to find the verdicts — a toolkit whose own targets need that has not proven the thing it sells. `tests/test_makefile_gates.py` runs the fast target for real and holds the rest by a census over the Makefile, so a new loud target fails. (Makefile, CLAUDE.md)
- **`cc-godot-sandbox.sh` guards the library's boot function with no configuration** — `gdk_rebuild_import_cache` in command position is blocked out of the box, pointing at `make import-cache`; `gdk_sandbox_home` is deliberately NOT on the roster, because it is the door rather than a boot and blocking the one function that makes a run safe is how a guard gets switched off. The empty-by-default `SANDBOX_FUNCTION` stays as the escape hatch for a repo still carrying a project-prefixed spelling; both feed one roster and the BLOCK message names whichever entry matched. Stock corpus 11 block / 9 allow → **13 / 13**. (repo/installables/cc-godot-sandbox.sh)
- **`cc-godot-sandbox.sh` stops blocking a quoted `godot` word** — the segment split ran a `tr` over the whole typed line, cutting at `;` `|` `&` `(` `)` and brace/backtick, quotes and all, so a quoted word that happened to follow one of those characters became the next segment's COMMAND word: `echo "foo; godot --headless"`, `git commit -m "block (godot --headless) in command position"` and five more everyday commands were BLOCKED (measured against the shipped guard; a consumer forked the file 183 lines to fix it). The split now tracks quote state and breaks only on an operator OUTSIDE quotes, while a quoted COMMAND word (`"$GODOT" --headless`) still is one; an unbalanced quote or a line past 8192 characters falls back to the naive split, which is the STRICT direction, never looser (36KB with 4,000 operators: 12s → 0.28s). New `--self-test` replays the whole block/allow corpus through the real hook — **13 block / 13 allow**, measured on the tagged file (the 11 / 9 and 13 / 11 figures this line first carried both predate the roster change in the bullet above, and neither is what v0.19.0 shipped) — and consumers wire it into their static gate (`install-hooks` and the README say so). (repo/installables/cc-godot-sandbox.sh, repo/install.py)

## v0.18.0 — 2026-09-02

- **`pm new story` stops scaffolding a story `pm validate` rejects** — under `[pm] story_ordinal_prefix` the ordering prefix belongs to the FILE, never to `id:`, so `new story <f> 01-slug` now writes `id: <f>/slug` and V2 passes on sight (every story scaffolded in one consumer's tree had to be hand-fixed). One rule, one home: `model.story_slug_of` is what resolution, V2 and the scaffolder all read. A second file whose stripped slug claims a live id refuses instead of minting an id neither file can be addressed by. (repo/pm/cli.py, repo/pm/model.py, repo/pm/validate.py)
- **`refs` counts a receiverless call** — every call-site alternative required a leading `.`, which is how GDScript spells a call to ANOTHER object; a call to a method of `self` (same file, or a base method called from a subclass) is bare, so those reported ZERO references — the answer that gets a method deleted. Measured against nullbound: **205 of 1,811 public method names read as unused and are not**, call sites 7,075 → 8,074 tree-wide. The declaring line stays a definition and is not double-counted as a call; `_on_name(` and `other.name(` are unaffected. (godot/read/refs.py)
- **`pm decide` refuses a title a shell cut in half** — `make pm ARGS="decide <id> a; b"` expands UNQUOTED, so the consumer's shell hands this process `a` and runs `b` as a command of its own; one consumer's log took two half-headings that way. A title left DANGLING on `;`, `&` or `|` is now a refusal naming the quoting fix, and nothing is written (the log is not even minted). A metacharacter that survived the caller's shell — quoted, or escaped across two argv tokens — is a word, written verbatim. `pm --help` states the quoting rule. (repo/pm/cli.py)
- **`refs` refuses an empty or blank symbol** — every pattern is built around the symbol, so an empty one is a match-anything: it claimed 6,831 typed refs and 880 call sites in a consumer at exit 0. Now a one-line usage error, exit 2, no scan. (godot/read/refs.py)

## v0.17.0 — 2026-08-31

- **`pm` id resolution refuses what creation always refused** — dot/empty/separator segments and absolute ids: `0.1/..` no longer writes a milestone's decisions.md, an absolute milestone id exits 2 instead of a `Path.glob` traceback. The fuzz harness's three carve-out pins died with their reasons. (repo/pm/model.py)
- **the scene plane refuses a path the filesystem itself rejects** — ENAMETOOLONG (and kin) across scene set/rm/rename/add/reparent/connect, canonicalize, tiles paint/erase and refs --retarget is a refusal, not a raw OSError. (godot/write/)
- **`check uid --fix` is byte-surgical on CRLF files** — only the uid attribute changes, every line terminator survives (whole-file byte-compare proven). CHECK 1 now censuses Script ref uids permissively: an undecodable spelling is reported INVALID, never repaired — closing the gap where CHECK 5's Script exemption and `check tres` both waved it through. (godot/checks/uid.py)
- **a tracked-but-deleted file is a disclosed `UNVERIFIED` skip** in check uid/tres/props, never a FileNotFoundError traceback. (godot/checks/)

- **`make fuzz` grows a seeded adversarial-input property harness** — every pm grain id and every scene/retarget path either refuses byte-identically or writes only inside its named slot, over 12 hostile input classes with anti-degenerate censuses. Proven to catch the v0.16.0 traversal blocker against the pre-fix code; found and pinned three live findings on arrival (`pm decide` dot-segment traversal, absolute-milestone-id crash, over-long scene-path crash — filed as 0.17.0 bugs). SDLC §5 now requires every new input surface to ship a refusal matrix plus adversarial cases against its own docstring claims, mirrored into the verification-builder contract. (tests/test_fuzz_inputs.py, SDLC.md, repo/installables/verification-builder.md)

## v0.16.0 — 2026-08-30

- **`pm decide` refuses a `--`-leading title word** — the retired four-field interface's flags were landing verbatim in the durable log at exit 0. (repo/pm/cli.py)
- **pm templates stop minting machine-unread frontmatter** — `severity`, `labels`, `estimate`, `risk`, `size`, `theme`, `target_date`, `track` are gone from the stock grain templates (`pm set` inserts any of them back on demand); `bug.md` keeps `caught_in`/`fix_milestone` and its comment now describes `pm retire`, not the retired D14/prune. pm-operator.md's tooling contract matches the shipped verb roster — re-install with `--force`. (repo/pm/templates/, repo/installables/pm-operator.md)
- **`cc-godot-sandbox.sh` blocks godot-named-variable boots** (`$GODOT --headless`, `"${GODOT}" -e`), carries a `project config` header, and states its accepted arbitrary-name fail-open gap. (repo/installables/cc-godot-sandbox.sh)
- **post-review structure pass** — the two biggest check runners restructured along their phase/section seams with byte-identical output (differential-proven); the installer family extracted from pm/cli.py (1,374 → 1,090 lines); module-level `apply.move`/`remove_tree` deleted with their last readers; test_pm.py split by concern at identical collected-test count; UTF-8 refusals, doc-name and vendored-prefix literals each got one home. (src/, tests/)

- **`scene set --resource` / `--sub-resource <id>`** — the set verb reaches the whole `data/**.tres` plane, previously hand-edit-only; the id is verbatim what `scene --props` prints (read output is write input). An unknown id refuses naming the known ones. (godot/write/scene_edit.py, godot/format/tscn_document.py)
- **`scene add --instance res://x.tscn`** — instance nodes (`instance=ExtResource(...)`, no `type=`); the PackedScene ref is minted from the target's own uid and REFUSED when the target is missing or has no resolvable uid — never invented. (godot/write/scene_edit.py)
- **`scene connect` / `scene disconnect`** — `[connection]` sections were parsed and rewritten on rename/reparent/rm but never authorable. Connect appends in Godot's serialization position; disconnect removes exactly the match, refusing ambiguity (`--flags` disambiguates); connect→disconnect round-trips byte-identically. (godot/write/scene_edit.py)
- **`refs --retarget <old> <new>`** — the post-`git mv` repair: every `ext_resource` `path=` attr and exact `preload`/`load` literal naming old rewritten byte-surgically (uid attr untouched); a comment, substring, or quoted path outside a call is SKIPPED with the reason and exits 1 — a loud skip, never a silent rewrite. `--dry-run` lists every site. (godot/write/refs_retarget.py, cli.py)

- **new opt-in `check pm` rule D10** — a `building` milestone whose `branch:` is empty or equals `[repo_hygiene] mainline` (`origin/`-stripped) is drift. Pairs with D9: run D9 alone for "a building milestone declares its branch," add D10 for the stricter guarantee that it is never the trunk itself. Devkit enables it on its own tree; the flow it encodes is `SDLC.md` §1. (repo/checks/pm.py, repo/pm/model.py)

- **`pm bug <status> <id>`** — moves a bug's status through the vocabulary and resolver that already existed for it: the one grain a typo'd status could previously reach only by hand edit or untyped `pm set`. (repo/pm/cli.py)
- **`pm retire <milestone-id>`** — fills the ROADMAP.md table `pm init` already seeds: one whole-or-nothing write removing the milestone directory and appending its row, reporting (never refusing on) an undone status or live children; `--dry-run` decides and prints without writing. The `NoDeleter` guard survives narrowed to `cmd_retire`'s body — a named single target is not `prune`'s automatic sweep. (repo/pm/cli.py)
- **`pm move <story-id> <feature-id>`** — re-parents a story whole: renames the file and rewrites `id`/`feature`/`milestone` together through the new `model.set_fields`, or touches nothing. (repo/pm/cli.py, repo/pm/model.py)

- **`install-agents` ships the full base agent roster** — architect, po, developer, reviewer, milestone-reviewer, simplifier, test-writer, tech-writer, changelog-writer, doc-hygiene, pm-operator join the verification pair: generalized from the two consumers (the better half taken where the forks diverged; designer/world-designer/tools-reviewer excluded as genuinely project-bound), each with `model:`/`effort:` frontmatter carrying the tiering mix and an editable Project config section. The SDLC they run — milestone branching, the scout-once dispatch loop, the model mix, the token-economy rules — is codified at the repo root as `SDLC.md`. (repo/installables/, repo/install.py, SDLC.md)

- **`check uid` runs five checks** — CHECK 3: a NEW (untracked or staged) `.gd` with no `.uid` sidecar on disk is a finding naming the mint remedy — the tracked-only census missed the exact moment of risk (trail's written ask; supersedes both consumers' `cc-uid-sidecar.sh` hook). CHECK 4: a tracked `.gd.uid` whose `.gd` is gone is a finding; `--fix` deletes it. CHECK 5: every header and non-Script `ext_resource` uid must be the canonical `ResourceUID` spelling; `--fix` canonicalizes byte-surgically (same id, no ref break); an undecodable uid is reported, never repaired. Output adds a census disclosure line and per-class `FIX —` lines; every existing line shape unchanged. (godot/checks/uid.py)
- **pure-Python port of Godot's `ResourceUID` text codec** — base-34 with the engine's compatibility off-by-one, uint64 wrap, 63-bit mask; validated against 3,018 real consumer uids (37 real non-canonical spellings found) and an engine-adjudicated round-trip pair. Folds nullbound's Godot-booting `resource_uid_scan.sh` into the no-boot gate. (godot/index/uid_codec.py)
- **`apply` grows `DELETE_FILE`** — single-file deletion joins the one place the package mutates a filesystem, with the same decided-before-any-byte obstructions. (core/apply.py)

- **`install-hooks` ships the whole agent-workflow guard corpus** — stop gate, write confinement, pre-push, prepare-commit-msg, agent-worktree, doctor: previously ~1,800 lines forked across the two consumers, now 925 generic lines, each standalone with an editable `project config` header. Generalizing fixed a shared pre-push defect (a substring match blocked any branch *containing* a protected name) and unified the forks' one-sided fixes: same-repo sibling-worktree writes allowed (nullbound's fix), non-repo scratch writes allowed (trail's fix), PM-tree integration-branch base for agent worktrees. (repo/installables/, repo/install.py)

- **`scene --props` renders `[resource]` and `[sub_resource]` property VALUES** — previously key names only. Packed/bulky data elides as node props already do, resource-ref arrays stay whole as `[→id, …]` (inside a resource body the ids ARE the structure), and every sub_resource id prints verbatim — the address `scene set --resource/--sub-resource` takes. Default output unchanged except a new `## resource` block on `.tres` files; existing lines byte-stable, proven differentially across the 65-file corpus. Trail's `content_schema_lint.py` SEAM parser can retire against this. (godot/read/scene_summary.py)

- **a committed 65-file scrubbed corpus proves round-trip fidelity in CI** — 36 nullbound + 29 trail `.tscn`/`.tres`, prose deterministically anonymized with structure byte-preserved, covering 24 distinct awkward constructs (tile_map_data, editable paths, packed arrays, inline comments, escaped quotes, …). The corpus round-trip runs unconditionally — CI no longer proves only the hand-built fixture — with a census floor and construct-coverage guard so it cannot rot. (tests/fixtures/corpus/, tests/test_tscn_roundtrip.py)

- **the hard rules are gates on this repo now** — three new boundary tests: config imports live on a closed allowlist and no collection may be built directly from a config lookup (the v0.9.0 bare-string bug class, now unwritable); zero dead imports across src/ (found and removed the last two); the layer directions (format→index→read/write→checks, repo↛godot, core↛both) are asserted with per-layer census floors. Each proven to fire on injected probes. (tests/test_boundaries.py)
- **one home per helper** — the thrice-declared diff renderer, the twice-declared value formatters and section constants, and nine cross-module underscore imports are collapsed to single public homes, proven byte-identical against the old bytes on real CLI runs. (godot/)
- **doc hygiene** — README pins bumped to v0.15.0 and both pin sites named in the release skill; `check doc`'s docstring stops describing its pre-extraction life; `uv.lock` ignored; the Makefile's no-venv comment scoped to the gates. (README.md, .claude/skills/release/, repo/checks/doc.py)

- **the uppercase-migration machinery is retired** — both consumers completed the lowercase grain migration, so the temp-suffix rename pre-pass, `_rename_case` and `git_rename` are deleted (−210 source lines, −208 test lines). A legacy uppercase slot is now a whole-grain refusal naming the variant and the exact `git mv` to run — never a silent rename, twin, or truncation. (repo/pm/templates/, repo/pm/model.py)

- **write verbs preserve the file's own line endings, byte-for-byte** — load/save normalized every line to the platform ending (universal-newline read + `os.linesep` write), a whole-file rewrite the `--dry-run` diff and the "(N line(s))" count never showed. Lines now carry their endings through the store: an untouched line keeps its exact bytes, an in-place rewrite keeps the replaced lines' endings, an inserted line takes the file's dominant ending. Proven on 1,406 real consumer `.tscn`/`.tres`: `load().text` byte-identical to disk on every one. (godot/format/tscn_document.py, godot/write/)
- **`scene rm` refuses a path that resolves nothing** — a typo'd path was indistinguishable from success at exit 0; it now refuses with the file untouched, and `--force` restores the no-op for scripted re-runs. Non-UTF-8 input is a printed refusal in all three write verbs, never a traceback; a root name shared with a child is an ambiguity refusal instead of a silently wrong node. (godot/write/scene_edit.py)
- **`apply` stops lying twice** — DELETE_TREE swallowed every error (`ignore_errors=True`) and reported the step landed; a real failure now surfaces, an already-gone tree stays a landed no-op. The case-only-rename carve-out (`upper()`/`lower()` name match) both false-blocked mixed-case renames and could silently overwrite a *different* file on a case-sensitive FS; it is now a same-file respelling check. (core/apply.py)
- **`scene-diff` addresses the root as `.` and exits 2 on a git error** — the root row was keyed by name (a root rename rendered as remove+add, and the row wasn't valid write-verb input); an unservable ref exited 1, the code reserved for findings. (godot/read/scene_diff.py)
- **`format/` no longer imports upward** — the uid resolver is injected from `write/`; an AST test pins the layer direction. `--elide-defaults` compares big ints exactly instead of through `float()`. (godot/format/tscn_document.py, godot/index/resource_defaults.py)

- **`extra_properties` is scoped to its class key** — the documented per-class carve-out was applied to EVERY class (the key discarded), so a typo'd assignment of a carved-out name anywhere false-PASSed `check props`; a bare-string value iterated characterwise into 12 one-letter legal names. The key now must name the section's engine type or script `class_name` (ancestors included), values go through the new `str_tuple_table` guard, and the key is documented under `[props]` (it was shown under `[defaults]`, which never read it). (godot/checks/props.py, core/config.py, README.md)
- **`refs`, `orphans` and `autoloads` read `devkit.toml` instead of one consumer's layout** — `[refs] exclude_prefixes`, `[orphans] vendored_prefixes/entry_point_prefixes/auto_discovered_prefixes/convention_files`, and the `[autoloads]` keys all resolve at call time through the core guards, with stock defaults byte-identical to the old hardcoded rosters (differential-proven on both consumers). A bare-string or wrong-typed value is exit 2 by name — `expected_prefixes = "autoloads/"` used to become a character tuple that silently suppressed layout flags. Six dead `load_config` imports removed. (godot/read/, godot/checks/)
- **read paths refuse instead of stack-tracing** — missing `project.godot` (autoloads, orphans), running outside a git repo (orphans), a non-UTF-8 file mid `check uid --fix` (drift stays reported, file never lossily rewritten), and a tracked-but-locally-deleted file during uid indexing. (godot/read/, godot/checks/uid.py, godot/index/uid_index.py)

- **`pm` refuses instead of crashing or lying, four ways** — a non-UTF-8 grain file stack-traced `pm sync` and V6 mid-tree (later findings unreported); reversed/lone `OPEN`/`CLOSE` markers hit the append branch every run, growing the file forever at exit 0; `pm status` over an empty or misconfigured roadmap printed nothing at exit 0; `pm sync --check` over zero grains printed `all 0 … current` at exit 0. All four now refuse naming the file, the markers, or the scope problem. (repo/pm/execlist.py, repo/pm/cli.py, repo/pm/validate.py)
- **the status verbs honour a custom vocabulary everywhere** — `feature done`/`feature review` dispatched before the vocabulary check, so a repo with custom `feature_states` got D4 drift written by the sanctioned tool; `--cascade` wrote story `done` without checking `story_states`; `--review-record=` (and an empty space-spelling value) silently skipped the stamp. Target states now validate at exit 2 naming the set; an empty review record is a refusal. (repo/pm/cli.py)
- **one home per fact in the pm internals** — `VALIDATE_RULES` (a byte-identical twin of `model.VALIDATE_CHECKS`) and the dead `include_archive` parameter + unreachable archive-merge branch are deleted. (repo/pm/validate.py, repo/pm/model.py)

- **`cc-commit-pathspec.sh` recognizes `--pathspec-from-file`** — both spellings were false-BLOCKED: the space form consumed the file argument without setting the verdict, the `=` form fell into the generic flag skip. A pathspec by file IS naming your paths; it now waves through, and the header's exemption roster says so. (repo/installables/cc-commit-pathspec.sh)
- **`cc-godot-sandbox.sh` detects every boot shape, not a flag roster** — `godot -e`, `godot <scene>`, `godot .`, bare `godot` and `timeout 60 godot -e` all reached a real engine boot against the real `user://`; a `--help` substring anywhere waved a boot through; a `<<<` herestring truncated the scan ahead of a following boot. Detection is inverted to a query-only allowlist (a godot in command position is allowed iff every argument is `--version|--help|-h`), herestrings are neutralized before the heredoc strip, and `command -v godot` stays exempt. Proven by 36 installed-hook payload tests, 14 of which fail against the old bytes. (repo/installables/cc-godot-sandbox.sh, tests/test_hooks_payloads.py)

## v0.15.0 — 2026-08-30

- **BLOCKER: `pm feature done --cascade` was unreachable from the state the plain run leaves you in** — The verb short-circuited on an already-`done` feature BEFORE the cascade, so the two-step its own output prints as the remedy ("`--cascade` closes the ones at `review`") answered "already done (no-op)" at exit 0, moved no story and stopped even reporting the untouched ones. The feature flip is now the only idempotent part: the cascade, the `--review-record` stamp and the untouched-story report run whatever the feature's status is, and a late record on a closed feature has to RESOLVE like any other. (repo/pm/cli.py)
- **eleven config surfaces stop being silently ignored** — `review_min_content_bytes`, the three grandfather ledgers, the six line caps and the whole `[agents]` section were all read from `devkit.toml` at v0.14.0 and by nothing at HEAD, yet `check pm` and `pm validate` both printed PASS at exit 0 over a file declaring every one of them. They are now named at exit 2 beside the six already in the ledger — on the GATES only, so the read verbs keep working through the pin bump. `core.config.section_declared` catches an empty `[agents]` too, which `config_section` cannot tell from an absent one. (repo/pm/model.py, core/config.py)
- **`pm list --milestone` names the set** — A typo printed `0 of 0` at exit 0, indistinguishable from an emptied milestone and from a wrong `roadmap_dir`. Milestone ids are enumerable, so an unknown one is exit 2 listing them, the way `--status wombat` already did; an empty roadmap says it is a scope problem rather than a typo. (repo/pm/cli.py)
- **the boundary allowlists cannot pass on an empty census** — Both assert an EMPTY offender list, so a moved `SRC` would have satisfied them over zero files. `_sources()` now refuses a census under 20 modules, and a test proves the refusal by pointing `SRC` at an empty directory. (tests/test_boundaries.py)
- **five shipped claims the cuts made false** — The cascade is described as OPT-IN everywhere it is described (`cli.py` module docstring, `pm vocabulary` text and `--json` `notes.feature_done`); exit 1 is named as a refusal, not "findings"; `pm new milestone --help` stops promising directory slots; `check pm` says its real default roster (D1-D6 + V1-V5) and stops calling the feature close atomic; `make smoke` is described by the verbs it actually runs in README, CLAUDE.md and `make help`; and `devkit.toml`'s `[pm]` comment stops describing deleted ratchets. (repo/pm/cli.py, repo/checks/pm.py, README.md, CLAUDE.md, Makefile, devkit.toml)

- **the two handoff docs match the package** — README rewritten 655 -> 375 lines, opening with what the tool IS (informs / scaffolds / edits) and the pinned-tag sharing loop instead of a taxonomy. Dropped the false "precondition-checked CLI" and "a status you move through code" claims for closed states, open transitions; removed the `[agents]` config section and `check agents` from the roster, the duplicated northstar block, and the `python3 -m unittest` development stanza (the suite is `make test`). CLAUDE.md rule 2 loses its archaeology parenthetical. (README.md, CLAUDE.md)
- **consumer smoke is a target** — `make smoke` runs every read verb against the live game checkouts, compares each printed census against an independent count, and fails if it leaves either checkout dirty. (tools/consumer_smoke.py)
- **one walk** — Filesystem enumeration moves into core/walk.py, which returns what it KEPT and what it SKIPPED under a closed-enum reason; Walk has no length, so a census cannot reach a count without its disclosures. (6ac90c3)
- **the two allowlists** — An AST test asserts glob/rglob/iterdir/os.walk live only in core/walk.py and write_text/open-for-write/rename/unlink/rmtree/mkdir only in core/apply.py, naming file:line otherwise. (tests/test_boundaries.py)
- **one apply** — Filesystem mutation moves into core/apply.py: a Plan is an explicit list of Steps, decide() names every obstruction from a closed enum before anything runs, and Applied says which landed. (7134e72)
- **the toolkit stops managing your prose** — The prose ratchet (D17/D18, `pm collapse`, `pm prose-ledger`, six line caps) is gone. Its entire output over two releases was 41 lines of markdown. (b8dc7c4)
- **`pm decide` writes a heading, not a form** — The four-field decision schema produced ZERO conforming entries across a consumer's 158 decision logs. `pm decide <grain-id> <title>` now stamps the date and the next ordinal and stops; the reasoning under it is yours. (6de5b0e)
- **`pm new` stops minting empty files** — Scaffolding a `decisions.md` and `handoff.md` into every grain put 204 empty files and ~1,900 lines into one consumer's tree. A shared doc now appears on first WRITE. (b1c9629)
- **BREAKING: `pm changelog`, `pm decisions`, `pm collapse`, `pm prose-ledger`, `pm claim`, `pm release`, `task` and `check tasks` are removed** — No consumer invoked any of them. `pm set <id> owner <x>` replaces claim/release; `CHANGELOG.md` is hand-maintained. (4e034a4, 56ffaf4)
- **`install-ci` and `install-agents` are back** — They were cut on a census reading 0 consumer references; the verbs had shipped hours earlier and had been held read-only against both consumers, so the census measured a restriction rather than disuse. Restored from history. (56ffaf4, src/godot_devkit/repo/install.py)
- **`install-ci` emits ONE opinionated workflow** — checkout, uv, `make milestone`. The `[ci]` config block, its hand-rolled TOML→YAML emitter and the PyYAML dev dependency do NOT come back: a project that wants a different workflow edits the file, which after the write is its own. The assumption that `make milestone` exists is a comment in the emitted file, not a discovery mechanism. (src/godot_devkit/repo/installables/ci-verify.yml)
- **`install-hooks`** — The shared-tree commit guard, the raw-engine-boot guard and `setup-hooks.sh`, which were forked between two consumers and drifting. Canonical here, each script STANDALONE: the project-name-prefixed JSON helper (`<project>_hook_json_field`, defined in a `_scope.sh` this package does not ship) collapses to one neutral `hook_json_field` defined where it is used, because a `source` of a library a fresh repo lacks fails open and a guard that fails open is not there. (src/godot_devkit/repo/installables/)
- **`--diff` on every install verb** — Prints a unified diff of what a run would change and writes nothing; an absent destination is named as an addition, an undecodable one as a whole-file replace. (src/godot_devkit/repo/install.py)
- **an install refuses on ANY difference** — The install-* verbs used to overwrite, without asking, any destination still carrying their generated header — so a project that edited an installed file lost the edit on the next run. A destination that exists and is not byte-for-byte what would be written is now refused by path, with `--force` named. (src/godot_devkit/repo/install.py)
- **BREAKING: D11, D12, D15, D16, D17 and D18 no longer exist** — `[pm] checks` naming any of them is exit 2. Neither consumer enabled one. Of the rules that survive this release — D1-D6, D8, D9 and V1-V6 — every one is byte-identical; D7 and D10 are removed by their own entries below, and V6 is demoted out of the default roster (D1-D6 + V1-V5) rather than deleted. `review.md` is a permitted file rather than a mandatory slot. (355b671)
- **`actual_date:` is not minted and not stamped** — `pm milestone done` stopped writing it and the milestone template stopped minting the field when the changelog render that read it was removed. Git records when a milestone closed; existing values in a tree are left alone. (src/godot_devkit/repo/pm/templates/milestone.md)
- **one gate roster** — `[checks] all` and the dispatch table were two lists holding one fact; a gate now carries its own default-in-`all` flag, and a test walks the roster to prove every name dispatches. (1c744f8)
- **BREAKING: `pm prune` is removed, with D7 and D14** — On the default roster an open bug under a `done` milestone was PASS, and prune then deleted it. Nothing here deletes a grain. (repo/pm/cli.py)
- **D4 covers a bug's status** — D14's one fact about a FILE (a status outside `bug_states`) moves to the rule that already owns it, on by default. `bug_open_states` goes with its only reader.
- **the census always counts bugs** — `N bug(s)` used to ride the opt-in D14, so a default run said nothing about a directory it had walked. (repo/checks/pm.py)
- **a stale rule id stops the gate, not the CLI** — One retired name in `[pm] checks` used to kill `pm status`, `pm get` and `pm vocabulary --json` too. Only `check pm` and `pm validate` refuse.
- **BREAKING: `check agents` is removed** — A1/A2/A4 failed a build because a markdown file DESCRIBED a workflow, and they inferred: a line's subject was guessed from "one grain word appears".
- **the flat-skill rule survives, in `check doc`** — `.claude/skills/<name>.md` instead of `<name>/SKILL.md` genuinely does not load. A fact about a file, on the gate that checks those.
- **BREAKING: the transition graph is removed** — README claimed "transitions no one can hand-edit around". A `sed` reaches the refused state and `check pm` prints PASS: nothing checked an EDGE.
- **closed states, open transitions** — `pm story|feature|milestone <status> <id>` takes any value in that grain's vocabulary; `butterfly` is exit 2 naming the set. `pm set <id> status` works too.
- **a status verb never validates the state it FOUND** — `pm milestone done` on a hand-edited `status: wombat` used to refuse. It prints `wombat -> done` and repairs the drift `check pm` reports.
- **BREAKING: the `pm feature done` story cascade is opt-in** — Without `--cascade` no story file is touched. Writing files the caller did not name is the tool acting on its own initiative.
- **BREAKING: `place_branch_on_building` and D10 are removed** — `pm milestone building` ran `git checkout` in your trunk worktree. A PM tracker does not mutate your VCS checkout. D9 stays.
- **a retired `[pm]` key is NAMED, never silently ignored** — `check pm` and `pm validate` exit 2 listing it, beside a stale rule id. The read verbs keep running, so a pin bump stays readable.
- **BREAKING: `review_min_content_bytes` is removed** — It refused an honest 15-byte "LGTM. Ship it." — the tool judging whether a person's prose was long enough. A record is a pointer that RESOLVES.
- **D1 is the dangling-pointer half only** — `reviewed:` naming a file that is not there, the shape V4 checks. "This feature has no `reviewed:` at all" is the absence of a document, not drift.
- **`pm feature review` and `pm milestone done` report instead of refusing** — Stories not at review, features not done: named in the output, and the verb does what it was asked. D3/D5 read the tree.
- **BREAKING: D13 is removed** — Its "extra slot" half failed your gate for keeping a file in your own milestone directory. The missing-grain-file half is already reported by `orphan_dirs` and V1.
- **V5 is the dependency cycle, and nothing else** — Phase-monotone fired on `seam`, a bucket this tool invented and called neither blocking nor blocked. `phase:` buckets; the graph orders.
- **`pm new` mints no directory** — `features/ bugs/ design/ stories/` were scaffolded empty, and git stores no empty directory: 158 `design/` dirs in one consumer, 11 with content in them.
- **`pm list` — the nail-finder** — One tab-separated row per story, filtered by `--status`/`--owner`/`--milestone`. `pm status` prints 165 lines on one consumer; the open work was 2 stories.
- **no `pm next`, on purpose** — A verb that picks THE next thing is the tool having an opinion about your priorities. `pm list` filters; you decide. `pm status` is unchanged.
- **V6 leaves the default roster** — A generated VIEW going stale while ordinary work moves the tree is not a defect in the tree. `pm sync --check` still answers; name V6 in `[pm] checks` to gate it.
- **empty repo to a closed milestone: 14 commands to 6** — Six of the fourteen existed only because an edge demanded them, and every one was reachable by hand anyway.
- **`pm vocabulary` prints the closed SETS, not edges** — The states each grain may hold and the rule ids `[pm] checks` may name, machine-readably. It is how a project sees what a pin bump changed.
- **a project's own template may open a grain at any state** — The guard forbidding it was the tool overruling a project about its own scaffold; D4 reads the state off the tree either way.

## v0.14.0 — 2026-08-29

- **a milestone has release notes** — `pm changelog <milestone-id> --what … --evidence …` appends a release note to that milestone's own log. (e432831)
- **the whole log, newest release first** — `pm changelog --render` prints the union of every milestone's notes to stdout, newest release first. (e432831)
- **read a grain's decisions back** — `pm decisions <grain-id>` prints that grain's decision entries, parsed — a milestone printing its own log and its features'. (e432831)
- **D15 holds a changelog entry to its schema** — D15 checks every `changelog.md` entry carries **What:** and **Evidence:**, with a `changelog_grandfather` ledger for legacy text. (e432831)
- **D16 stops a release shipping with no notes** — D16 fails a `done` milestone whose changelog is missing, empty, or holds only entries D15 already reports. (e432831)
- **existing milestones need one re-scaffold** — UPGRADE: `changelog.md` is a new canonical slot, so run `pm new milestone <id>` once per existing milestone to fill the gap. (e432831)
- **D17 caps grain prose as a ratchet** — D17 caps the line count of a story, a `feature.md`, a bug, a feature's `decisions.md` and a `changelog.md`, with the caps as config. (dcb2511)
- **D18 collapses a closed milestone's trail** — D18 fails a `done` milestone still carrying its raw decision trail — close evidence is pointers, not the log. (dcb2511)
- **pm prose-ledger, which will not raise a ceiling** — `pm prose-ledger` regenerates D17's debt ledger to stdout and REFUSES to raise an existing ceiling. (dcb2511)
- **a short --evidence says how to lengthen it** — A commit hash one character short of the minimum is now refused with the count, the minimum, and the `git rev-parse` that fixes it. (dcb2511)
- **a balanced backtick span no longer opens a fence** — A paragraph opening with a balanced inline code span no longer masks the rest of the document, which had hidden real findings from `check doc` and D12. (2eaae12)
- **a fence quoted inside a comment is not malformed** — A lone fence marker inside a paired HTML comment is no longer reported MALFORMED — the two markers hide each other and document order settles it. (2eaae12)
- **check agents reports what it cannot decode** — `check agents` reports an agent definition it cannot decode and takes it OUT of the scanned count, instead of dropping it in silence. (2eaae12)
- **stories/ is walked recursively** — A story at `stories/<subdir>/<name>.md` or named `.MD` is visible to every `check pm` rule, the way bugs already were. (2eaae12)
- **a grain is its frontmatter** — A `.md` under `bugs/` or `stories/` with no frontmatter block is a note parked beside the grains, not a grain with an empty status. (2eaae12)
- **pm new refuses instead of tracebacking** — `pm new` on an over-long grain name or an unwritable `pm/roadmap/` refuses with the reason, and nothing is written. (2eaae12)
- **check doc's FAIL line carries its census** — `check doc` names how many docs it read and how many fenced lines it skipped on FAIL, not only on PASS. (2eaae12)
- **check all runs the gates that apply to your repo** — New `[checks] all` in devkit.toml names the gates `check all` runs here; an unknown name is exit 2, never a quietly narrowed run. (src/godot_devkit/cli.py:87)
- **a nested story is addressable by id** — `pm story <state> <id>` resolves a story anywhere under `stories/`, so the gate can no longer report one the verb refuses. (src/godot_devkit/repo/pm/model.py:572)
- **[uid] exclude_prefixes scopes both uid checks** — `[uid] exclude_prefixes` now scopes the sidecar-tracking check too, not just the ref-drift one. (src/godot_devkit/godot/checks/uid.py:151)
- **an empty census says what it was out of** — A `uid`/`tres`/`props` run that scans nothing now reports "0 of N tracked", so an empty repo and an over-broad exclude stop reading identically. (src/godot_devkit/godot/checks/tres.py:54)
- **the toolkit runs its own gates on itself** — godot-devkit carries its own PM tree at `pm/roadmap/`, with every rule it ships enabled, and its own CHANGELOG rendered from it. (devkit.toml:1)
- **a release heading names its tag and its date** — A rendered release heading is now `## v<id> — <actual_date>`, matching the git tag it maps to; a milestone that has not shipped renders `## v<id>` with no invented date. (src/godot_devkit/repo/pm/cli.py:1218)
- **a broken grain is reported, not dropped** — A story or bug whose frontmatter is damaged — a BOM, a blank line above the fence, no closing fence — stays in the census and is reported, instead of leaving the scan in silence. (src/godot_devkit/repo/pm/model.py:706)
- **the census says how far it looked** — check pm now names how many .md files the grain walk skipped as notes, so a zero bug count is never mistaken for an empty directory. (src/godot_devkit/repo/checks/pm.py:540)
- **a milestone stamps its release date at close** — pm milestone done writes actual_date, which is what puts the date in the changelog render's '## v<id> — <date>' heading; the render still never reads a clock. (src/godot_devkit/repo/pm/cli.py:425)
- **pm prose-ledger names what it absorbs** — Regenerating the prose debt ledger prints every newly absorbed document on stderr with a count, so new debt can no longer enter the ratchet unannounced. (src/godot_devkit/repo/pm/cli.py:1394)
- **a version-shaped changelog heading is not an entry id** — A preamble heading like '## v0.9 release notes' no longer reads as entry 'v0', so the next appended note is allocated C1 rather than v1. (src/godot_devkit/repo/pm/model.py:1138)
- **one fact, one finding, in the right vocabulary** — An over-cap finding names the grain kind it is about, and a ledgered document back inside its cap is reported once — drop the entry — rather than twice. (src/godot_devkit/repo/pm/model.py:1900)
- **a hidden document is still disclosed** — A dot-prefixed file under bugs/ or stories/ is counted in the census instead of vanishing from it. (src/godot_devkit/repo/pm/model.py:769)

---

`v0.13.0` is the only historical section retained, for one reason: it carries
the breaking `DECISIONS.md` -> `decisions.md` rename and how to absorb it, and
the `trail` consumer is still pinned below it. When trail migrates it goes.

## v0.13.0 — 2026-08-29

**One uniform grain structure, all lowercase.** Every milestone and feature dir carries the same
slots, and the split that makes it worth having is durable vs transient:

```
<milestone>/                       <feature>/
  milestone.md                       feature.md
  handoff.md                         —          (milestone-only)
  decisions.md                       decisions.md
  review.md                          review.md
  bugs/                              —          (milestone-only)
  design/                            design/
  features/                          stories/
```

`decisions.md` is **durable** — appended during the grain's life, it survives close and collapses to
pointers when a milestone closes. `review.md` is **transient** — simplifier and reviewer both append,
and it is **deleted at close** with anything durable promoted into `decisions.md` first.

**`pm new milestone` and `pm new feature` are now idempotent**, which is how a consumer migrates. Run
against an existing grain they fill the missing slots, rename a slot present under another case, and
leave every existing byte alone; `<name>` is optional there, since the name only ever mints the
directory. The case rename goes through **`git mv --force`** when git tracks the path, and through a
temp name when it does not. Both halves are load-bearing and neither is optional: macOS is
case-INSENSITIVE, so `open('decisions.md', 'w')` next to an existing `DECISIONS.md` truncates the
very content the migration exists to carry forward — and git's default there is
`core.ignorecase = true`, under which a worktree-only rename leaves the INDEX on the old spelling.
The worktree says `decisions.md`, `git ls-files` says `DECISIONS.md`, and an explicit `git add` of
the new name stages nothing: the migration goes green on the laptop, gets committed, and CI on Linux
checks out the old name with D13 reporting every renamed grain missing and D12 scanning nothing. If
git tracks the path and refuses the move, the scaffolder **refuses too**, printing the exact command
— a half-done rename is the one outcome worse than none. A file slot that exists as a DIRECTORY is
likewise refused rather than crashed, because exit 1 is reserved for findings; that refusal reads the
KIND of every spelling, not just the canonical one, so a `DECISIONS.md/` directory is refused where a
name-only variant scan would have queued it as a rename, renamed it, and then opened it as a file.
A slot that is a SYMLINK is refused for the same reason from the other side: a verb asked to fill
THIS grain does not follow a link and rewrite a file outside it.

Every refusal the grain can raise is decided **before the first rename runs**, so `nothing was
written` is a claim about the whole grain rather than about the slot the refusal happened to land on:
the slot order is `milestone.md, handoff.md, decisions.md, review.md`, and a refusal keyed on
`decisions.md` used to leave `HANDOFF.md -> handoff.md` on disk and staged, where it rides out on the
next commit under somebody else's message. That now covers a leftover `.pm-case-rename` temp file
too — it is in the directory listing, so it is decidable in the pre-pass, and deciding it inside the
moving loop was the last way an earlier slot's rename reached disk under a "nothing was written".
**And the write phase is pre-decided the same way**: every template the grain will need is loaded and
DECODED, and every existing doc due a header prepend is proved writable, before the first byte lands
— a latin-1 byte in a project's `template_dir` and a read-only legacy `handoff.md` both used to
escape as tracebacks with two slots already created. What genuinely cannot be inspected in advance —
git declining a `git mv --force`, a mode changed underneath the run — becomes a refusal that names
what already landed instead of claiming nothing did, and says whether a landed rename was **staged by
`git mv`** or only moved on disk: advice to unstage a worktree-only rename sends an operator to a
`git status` that shows them nothing. The two halves of that composed sentence no longer contradict
each other either: the inner refusal states what became of ITS OWN file (*its content is untouched,
still at …*) and the NOTE states what became of the ones before it, where one message used to say
both `nothing was written` and `1 earlier rename(s) already landed`.

**Exit 1 is a finding, not a stack trace, on every path a rename takes** — the last two paths out
from under that are closed. A rename writes the DIRECTORY, not the file, and the pre-pass checked
only the file: a `0555` grain dir holding a `0644` `DECISIONS.md` passed every inspection and came
out of `os.rename` as a raw `PermissionError` traceback. Directory writability is trivially inspectable, so it is inspected,
and anything still escaping the two-step temp rename becomes a refusal that **names where the bytes
actually are** — the second step failing parks the log at `decisions.md.pm-case-rename`, which no
later run looks for, so a message saying only that the rename failed left an operator hunting for
content sitting right there. A SYMLINK in a file slot is also identified as a link before its kind is
read: `is_dir()` follows the link, so one pointing at a directory got the DIRECTORY refusal — the
right answer with advice aimed at the wrong problem. Measured on
scratch copies of both consumers, with a second full pass changing nothing:

| | grain dirs | slots created | renamed | headers restored |
|---|---|---|---|---|
| nullbound | 158 | 469 | 60 | 60 |
| trail | 32 | 132 | 4 | 4 |

**Each shared doc opens with a one-line instruction**, and D13 asserts it is still there. `.claude/
rules/*` never reach a dispatched subagent — measured — so a file's own first line is the one
delivery channel with a 100% hit rate for the action its reader is about to take. Each line is an
instruction for that action, not an explanation of what the file is:

- `decisions.md` — *Append with `godot-devkit pm decide <grain-id>` — never by hand; the command
  stamps the date and the next ordinal.* It points at the command rather than restating D12's four
  fields: the gate already owns that schema, and a second copy in 178 files is a drift generator.
- `review.md` — *Transient. Deleted at close — promote anything durable into decisions.md first.*
- `handoff.md` — *Cold-start only. Never restate what `pm status` computes.*
- `milestone.md` / `feature.md` get none. V1–V6 already validate their frontmatter.

**New — `pm decide <grain-id> --chose … --over … --because … --evidence … [--title …]`.** Appends a
D12-conforming entry to that milestone's or feature's `decisions.md`, stamping the two things authors
get wrong: the ISO date, and the next ordinal (in the log's OWN id prefix, so a tree numbering `M27`
keeps numbering `M`). `--over` is **required** — a decision with no rejected alternative is a
description, and a required flag enforces that at write time, where the author still remembers the
alternative, instead of at gate time weeks later. Every value is validated by re-parsing the composed
entry through D12's own predicates, so the writer refuses exactly what the gate would report and the
two cannot drift; prose evidence, an over-long value and a `--chose` too long to serve as the header
title are all refused with the log left byte-identical.

**`check pm` D11 is rewritten around the co-located `review.md`: a `done` grain must not have one.**
No `review_dir`, no filename matching, no exemption, no ambiguity. What it replaces resolved a
findings-doc FILENAME back to the grain it "named", and a real corpus got that exactly backwards — on
trail it resolved 6 of 123 docs, and those 6 were precisely the durable ones `reviewed:` already
points at, so after the previous release's fixes trail reported **0 findings over 123 stale docs**.
Anchoring the match could only ever remove matches. `grain_named_by()` and the `KNOWN_DEFECT` test
that pinned its substring bug are gone. The rule-4 loudness stays: a tree with no `done` grain is
NAMED in the output rather than passing in silence, and the census carries the done-grain count.

**`check pm` D13 — the canonical structure.** Every grain dir carries exactly its slots: **missing is
drift AND extra is drift**, and each templated file must still open with its instruction header so
the breadcrumb cannot rot. The extra half is the one that earns the rule — `plans/`, `findings/`,
`AUDIT-REPORT.md`, `audit-prompt.md` and `DELETED-SCENARIO-LEDGER.md` all exist in a real tree
because no slot was scaffolded *and* nothing flagged the invention, and a missing-only check leaves
every one of them there forever. Existence is decided from a directory LISTING, never `Path.is_file`:
macOS resolves `decisions.md` to an existing `DECISIONS.md` and Linux does not, so the same tree
would be clean on a laptop and drifting in CI. Directory slots are permitted, never required — git
does not store an empty directory, so requiring `design/` would mean 178 placeholder files.
`review.md` is required exactly while the grain is open and forbidden once it is done, D11 owning
that half, so a closed grain is never told both to have it and to delete it.

**`check pm` D14 — bug lifetime.** A bug lives in the milestone that will FIX it: `caught_in:` keeps
the provenance, `fix_milestone:` names the decision, and the directory is that decision made real. An
**open bug under a `done` milestone** is therefore drift, and not cosmetically: `prune`'s lag-by-one
deletes a done milestone's directory the moment the next one closes, so those bugs are already
scheduled for deletion. This rule is what makes prune safe by construction. It also reports a bug
status outside `[pm] bug_states` — D4 does not cover bugs, so a typo would otherwise read as "closed"
and pass in silence. Two new config keys, `bug_states` (default `open`/`fixed`/`closed`) and
`bug_open_states` (default `open`); naming an open state the vocabulary lacks is a config error.
The bug census is **recursive and case-insensitive in the extension**: a `glob('*.md')` saw neither
`bugs/<topic>/<bug>.md` nor `<BUG>.MD`, and since `bugs/` is a permitted slot that D13 never descends
into, both were invisible to every rule at once while the census printed the smaller number without
saying it had looked less far. D14 is the rule that stops `prune` deleting an open bug along with its
done milestone, so one that undercounts is not a weaker safety net — it is a false one.

D11, D13 and D14 are OFF by default like D8–D12 — a tree predating the canonical slots is missing
most of them, and a rule that turns a consumer red on upgrade day is unshippable. Scaffold first,
then hold the line. Measured on scratch copies:

| after scaffolding | D13 missing | D13 extra | D13 header | D11 stale | D14 open-bug-under-done |
|---|---|---|---|---|---|
| nullbound | 0 | 12 | 0 | 0 | 28 of 91 bugs |
| trail | 0 | 10 | 0 | 0 | 8 of 13 bugs |

The residual extras are the genuine inventions a human has to place: `plans/` (8 + 10),
`AUDIT-REPORT.md`, `audit-prompt.md`, `findings/` and `DELETED-SCENARIO-LEDGER.md`.

**`check pm` D12 — the decision-record schema.** Every `## <ID> — <ISO date> — <title>` entry in a
`decisions.md` carries `**Chose:**` / `**Over:**` / `**Because:**` / `**Evidence:**`, in that order,
one per line, values <= 200 chars and the title <= 80. `Over:` is the load-bearing field — an entry
that cannot name what it ruled out is a description, not a decision — and `Evidence:` must be a
REFERENCE (a commit hash, a `path[:line]`, a number), never a sentence.

Entry DETECTION reads the entry's BODY, never only its title: a `##` heading is an entry if it
carries an id or a date **or** if any `**Word:**` field line appears beneath it. The field line is
the positive signal, and it is what a title-reading detector cannot see — this package's own retired
template told authors to write `## <short title>` with `**Decision:/Because:/Rejected:/Costs:**`
underneath, and against trail's live corpus a heading-only test called 8 of 9 real decision blocks
prose and passed the whole file, its single finding landing on the one heading that happened to
contain a bug id. A heading with neither an id/date nor a field line is prose and is never
schema-checked, so a log may open with a preamble. A CLOSED `<!-- -->` block is not in the log at
all, and neither is a TERMINATED fenced code block — both are text the entry parser steps over. Its
two edges are symmetric and both keep their live half: the opening line keeps what precedes `<!--`,
and the closing line keeps what follows `-->`, so a conforming `**Over:**` written after a spanning
aside is still read rather than failed for naming no rejected alternative.

**Neither marker may mask in silence.** An unclosed `<!--` suppresses nothing and is itself reported,
and **so is an unterminated code fence** — one stray ` ``` `, a `~~~` "closed" by ` ``` `, an
unbalanced three-backtick span leading a line. Either would otherwise mark every line after it
dead and D12 would print PASS over the entries it ate, which is the exact sin the comment scan
already refuses to commit; the fence masking added to stop a quoted `<!--` eating a log had reopened
it by the other route, silently, and `pm decide` then refused every append to that log forever with
`the composed entry does not parse as a decision entry`. A marker inside backticks or a terminated
fence is a marker being named, not a comment being opened.

**And `check doc` / `check agents` now answer an unterminated fence the same way**, because it was
the same defect wearing the other gate's clothes: their fence scan was a parity toggle, so an ODD
number of fence-looking lines dropped every remaining line of the document and the gate printed PASS
over them. Two dead claims that FAIL normally — `[check:doc] FAIL — 2 unresolved claim(s)` — became
`PASS — 1 doc(s), 0 unresolved claims` the moment one stray ` ``` ` was prepended above them, and
`check agents` did it too. `check doc` runs in `check all`; both run from each consumer's
`make check`. An unterminated fence now masks
nothing and is REPORTED (`malformed doc(s)` / `MALFORMED`) while a terminated one still masks, since
a rule file quoting the CLI's own refusal is documenting it. **One scanner** answers where the fences
are — `core.markdown.fenced_flags`, which D12's own mask now calls rather than keeping a second copy
— so the two families cannot drift into disagreeing about which lines a document even has. That also
brings the CommonMark rules the toggle never had: a closing fence must be the same character, at
least as long and carry no info string (so `~~~` no longer "closes" a ` ``` `), and a fence indented
four spaces is INDENTED CODE — a doc showing how a fence is written no longer opens one. Both
censuses now print **how much they skipped** (`22 doc(s), 223 fenced line(s) skipped`): the count was
of FILES, and files are not what a fence hides.

Legacy logs migrate through `[pm] decision_grandfather` — `"<path>"` exempts a whole log,
`"<path>:<N>"` only its first N entries — whose size the gate PRINTS every run and which may only
shrink: an exemption that suppresses nothing, a cap reaching past the end of its log, and a line
naming no log all FAIL.

**D12 prints its census — `N decision log(s), M entry/ies` — and carries both into the summary
line,** the way D11 prints its done-grain count and D13/D14 print theirs. Without it "scanned 58 logs
/ 294 entries", "scanned 1 log" and "scanned 2 logs / 0 entries" printed identically, which is what
kept the two defects above invisible. The log census itself comes from a directory LISTING with an
EXACT-name comparison, never `rglob('decisions.md')`: a glob whose final segment holds no wildcard
resolves through `Path.exists()`, so macOS answers an on-disk `DECISIONS.md` with the path
`x/decisions.md` — a path that does not exist, a `decision_grandfather` key authorable on exactly one
platform, and, once ONE log of a tree has been migrated, a non-empty list that silences the
scanned-nothing guard while every other log goes unopened (57 of nullbound's 58, carrying 1,467
violations, printed `PASS` and exit 0). A case-variant log is now REPORTED, never folded in; a log
that cannot be decoded is REPORTED rather than counted as scanned-with-zero-entries.

**`pm templates` no longer writes past a case variant.** It reads the target directory by exact name,
exactly as the template loader does, so a project holding a customised `DECISIONS.md` is told the
spelling to port it to instead of silently rendering from the packaged template — and the packaged
one is not written over it, since on a case-insensitive filesystem that write would truncate the
customisation.

**`decision_grandfather = []` is now legal**, and means what it looks like. Every `[pm]` key is
written so that a repo declaring the documented defaults behaves identically to one with no
`devkit.toml`; this was the single key that broke that contract, exiting 2 with *remove the key to
take the default* because an empty list usually means the reverse of what it looks like (`git
ls-files` with no pathspec is the entire repo). It does not here: this key is a LEDGER of exemptions,
so `[]` and the absent key both say "none exempt".

**Known issues, all reported and none fixed here:** `pm decide --title` is not validated for a bare
`\r`; a `decision_grandfather` cap names the wrong entry when one is inserted above it;
`ScaffoldRefused` prints an absolute path rather than a repo-relative one.

Six more were reported here and all six are fixed in `## Unreleased` above: the balanced
three-backtick span read as a fence opener, `check agents` skipping a non-UTF-8 definition in
silence, `stories/` never getting D14's recursion, `pm new`'s unguarded `gdir.mkdir`, D14 reporting a
non-bug `.md` under `bugs/`, and an unterminated fence inside a *closed* HTML comment reported as
malformed.

**`tiles --region` and `tiles --at` can address the negative quadrants again.** `--region -2,-2,1,1`
died with `argument --region: expected one argument` on **every Python a consumer actually runs**:
argparse before 3.14 excuses a leading `-` only for a bare number, so a coordinate list read as an
option. Godot cell coordinates are routinely negative — `tile_map_data` signs x and y precisely
because the upper-left quadrant is ordinary — so `tiles paint` / `tiles erase` / `tiles --at` could
not reach a quarter of the plane on the declared 3.11 floor, while passing on 3.14. Fixed in the
argv, not the parser (the private matcher argparse keys on was rewritten in 3.14, and a tool whose
behaviour depends on which interpreter `uvx` picked is not a tool): a token opening `-<digit>` after
one of those flags is glued into `--flag=value`, on `sys.argv` as well as on an injected argv —
applying it only to the injected one left `python -m godot_devkit.godot.read.tiles … --at -3,-3`
exiting 2 on 3.11 while working on 3.14, which is the version-dependent tool the glue exists to not
be. Narrow on purpose — `--region --tile 9/0,0` is still a usage error with exit 2, and nothing after
`--` is touched. Predates v0.12.0. The suite now runs green on 3.11, 3.12, 3.13 and 3.14; it was
296 passed / 1 failed below 3.14.

**Breaking for a tree that has one:** the decision log is `decisions.md`, not `DECISIONS.md`, and the
handoff is `handoff.md`. `pm new milestone <id>` / `pm new feature <mid> <slug>` performs the rename.
`pm new feature` scaffolds `design/`, not `plans/`. The bug template carries `fix_milestone:` in
place of `fixed_in:`.
