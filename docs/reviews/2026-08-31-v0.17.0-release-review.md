# v0.17.0 pre-release review — RELEASE-SAFE

First release in this package's history where the pre-release review found no false-PASS blocker;
the reviewer attributes this to the adversarial-input harness this release ships. Full diff
v0.16.0..HEAD read; both cardinal-sin surfaces attacked directly with live probes:

- UNVERIFIED skip buckets cannot turn a red gate green: real drift beside a skip stays exit 1,
  all-skipped trees hit the zero-census FAIL, counts exclude skips honestly. The same broken tree
  tracebacks on v0.16.0.
- INVALID Script uid: censused, never repaired, exit 1 — a confirmed silent PASS on v0.16.0,
  closed live.
- CRLF `--fix` byte-surgical (terminators byte-compared); the lone-CR skew guard fails loud
  (DRIFT re-reported, never FIXED).
- pm segment guards reject nothing either consumer's real tree uses (nested + flat bug slugs
  probed on both live checkouts; both trees clean after).
- Whole-release output byte-identical on both live consumers across six gates; all verdict changes
  red-direction only. Exit codes 0/1/2 held in every probe.

Non-blocking findings carried to the next milestone as bugs: (1) mixed lone-CR skew could land a
repair on a wrong in-range line holding the same needle — harden to needle-matching; (2) a
lone-CR-only tree prints DRIFT then "nothing to repair" in one run — wording contradiction, exit 1
preserved; (3) `check defaults` still tracebacks on tracked-but-deleted (the class 0.17.0 fixed in
uid/tres/props); (4) `scene canonicalize good.tscn missing.tscn` writes the good file before the
usage error on the missing one — per-file existence check inside the loop, pre-existing, SDLC §5's
first customer.

Suite 679 + 3,635 subtests green (reviewer's run); fuzz harness verified deterministic,
residue-free, anti-degenerate assertions real.
