# 1.1.0 — every-config-key-shows-its-shape: feature review

Reviewer run 2026-09-12 over `547cb8b..f149a3f` (lane commit `d184cd9`, and `1d4ad34`, which
reshaped production code for the census). Method: I read the census in `tests/test_boundaries.py`,
ran it in the full suite (210 passed, my run), and ran `check unit-disk --help`.

**Verdict: SHIP-WITH-FIXES.** The README block now carries `forbidden_literals` as a table and the
three `test_shape` keys that were missing. The census derives the key set from the guard calls
themselves, and feeds each example back through its own reader, so a mis-shaped example fails too.

## Blockers

None at MAJOR or above.

## Criteria

| criterion | status | shown by |
|---|---|---|
| every key the kit reads has an example of its shape in the README block | met | `EveryConfigKeyShowsItsShape` green; floors 8 readers / 20 keys |
| a census test fails on a key with no example | met | same test; `missing` and `misshapen` asserts |

Findings:
- `MN1` MINOR `src/godot_devkit/godot/checks/unit_disk.py:34-43`. `check unit-disk --help` still
  shows no `forbidden_literals =` line in its `devkit.toml` example, and the feature's own finding
  cites `--help` as half of the defect. Repro: `PYTHONPATH=src python3 -m godot_devkit.cli check
  unit-disk --help | grep -c 'forbidden_literals ='` prints `0`. A consumer who reads `--help`
  first still guesses the shape.
- `N11` NIT (lens zero): the census is about 150 lines of AST resolver for one assertion, and
  `1d4ad34` changed production code (a classmethod became a module function) so the resolver could
  follow it. A smaller end state may exist: the guards record `(section, key)` as they read, and
  the test runs the gates over a fixture with an empty config. Only raise this if the resolver
  needs another production change.

```text
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| MN1 | MINOR | landed 782cbee |
| N11 | NIT | rejected: a NIT, left as written for 1.1.0 |
```
