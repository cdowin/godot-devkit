# 1.1.0 — the gate measures what it costs: milestone review (cross-cutting)

Reviewer run 2026-09-12 over `547cb8b..f149a3f`, 30 commits. `a85e313` (the agentic-sdlc v0.2.0 ->
v0.11.0 pin) was skimmed and its installed files not reviewed. There are six feature records beside
this one plus bug `bg-uid-guard-triggers-on-staging`, which is reviewed here. The full suite ran on
3.11: 210 passed, my run. Probes ran under `$TMPDIR` scratch, in-process, because the harness blocks
`git init`/`git archive`. The harness also blocks a raw Godot boot, so no saver output was observed.

**Verdict: HOLD.** Three MAJORs stand open in the feature records, each a one-line or one-decision
fix:
- `MJ1` (canonical): exponent-decade floats are rewritten to positional form.
- `MJ2` (cost): the cost-row subshell recurses to fork exhaustion on a library that defines
  neither function.
- `MJ3` (baseline): `uid` and `canonical` take no baseline, against both ship criteria.

This record's own findings are MINOR and NIT.

## Across the lanes

- **Baseline × canonical.** The canonical lane added a ninth per-file gate after the baseline lane
  fixed its scope at six, so neither lane could see the gap. That is MJ3, raised in the baseline
  record. `check defaults` and `--respell` do not mask each other: a redundant `ids = []`, once
  respelled to `Array[int]([])`, is still REDUNDANT (scratch probe, 3/3 before and after).
- **Canonical × defaults docs.** Canonical made a claim in `defaults` false (MN3, below).
- **Install × cost.** The two-pin section pins agentic-sdlc v0.2.0 (MN2, in its record). The cost
  lane does not depend on it: v0.2.0's `gdk_gate.sh` already honours `GDK_GATE_VERDICT` and census
  (stub probe filed `--verdict PASS`).
- **Semver.** Nothing a consumer Makefile or hook must edit. The installed `Makefile.tiers` and
  `uid-guard.yml` stay the consumer's until `--force`. Every new output line is additive
  (`BASELINED`, `GREW`, `SHRUNK`, `STALE`, `[INTEGRATION] BOOTS:`). With no config the old shapes
  are byte-identical (72/72). Minor bump is right.
- **Rule 1 spirit.** A missing `GDK_GATE_LIB` is one stderr note, exit 0. A library that exits
  files nothing, exit 0. The only breaking case is MJ2.

## Criteria (milestone ship criterion)

| clause | status | shown by |
|---|---|---|
| any tier's cost is a number: boots for scenarios, a row for every other tier | met in this package; grading not verified | the feature records; agentic-sdlc not run |
| `test-shape --help` says readability gate and names cost | met | `check test-shape --help` |
| each of the eight gates can be turned on frozen | not met (7 of 8) | MJ3 |
| every documented config key has an example of its shape | met | `EveryConfigKeyShowsItsShape` |
| bug: uid-guard fires on `milestone/**`, and the staging claim is gone | met | `ci-uid-guard.yml:28`; `install.py:107,136`; the amended installable test |

Findings:
- `MN3` MINOR `src/godot_devkit/godot/checks/defaults.py:19-20` says "the other dimensions are not
  decidable by parse", which `check canonical` now does for respell and order. Point it at
  `check canonical`.
- `MN4` MINOR `src/godot_devkit/godot/installables/ci-uid-guard.yml:1` says
  "GENERATED by godot-devkit — `godot-devkit install-ci`". That verb exits 2 naming agentic-sdlc,
  and `install-runners` writes this file. The bug fix edited lines 9-13 of this header and left
  line 1. Repro: `PYTHONPATH=src python3 -m godot_devkit.cli install-ci; echo $?` prints `2`.
- `N12` NIT (rule 8): `milestone.md:15,27,35` and two feature files name two consumer projects.
  `pm/` is exempt from the sweep by design (`test_consumer_independence.py:52`), but CLAUDE.md
  rule 8 says "no file", and the shipped code already writes the shape ("measured on one consumer").
- Process: this pass staged `pm/roadmap/ledger.jsonl` (8 gate rows from 14:33-14:35, not this
  reviewer's) through a `git add -A` whose `cd` had failed. The allowlist refuses `git restore
  --staged` and `git reset`, so it is still staged. It is harmless under pathspec commits.

```text
verdict: HOLD
| id | severity | disposition |
| MN3 | MINOR | landed 782cbee |
| MN4 | MINOR | landed 782cbee |
| N12 | NIT | rejected: a NIT, left as written for 1.1.0 |
```

## Fix review — 782cbee (code-reviewer, 2026-09-12)

MJ2, MJ3, MN1–MN4 confirmed closed by probe. The MJ1 fix introduced R1: outside the new
[1e-3, 1e5) window a trailing-zero fraction (`0.00050`) was left alone and counted instead of named
— a false PASS. R2: a gate library without the call dropped the cost row silently. R3: no test
pinned MJ2. All three landed in 4d21215; `make test` 210/210 after.

```text
verdict: RELEASE-WITH-FIXES
| id | severity | disposition |
| R1 | MAJOR | landed 4d21215 |
| R2 | MINOR | landed 4d21215 |
| R3 | MINOR | landed 4d21215 |
```
