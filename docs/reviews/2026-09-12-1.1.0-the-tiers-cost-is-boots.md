# 1.1.0 — the-tiers-cost-is-boots: feature review

Reviewer run 2026-09-12 over `547cb8b..f149a3f` (lane commits `2bce17e`, `30b94b1`). Method: I read
`integration.sh`'s census and CPU accounting, `Makefile.tiers`' `GDK_CENSUS_BOOTS` and the
test-shape docstring, ran `check test-shape --help`, and used the suite's sweep case, which reads
the census back through the installed Makefile's own `sed` (full suite, 210 passed, my run).

**Verdict: SHIP.** The BOOTS line prints above SUMMARY, so `GDK_SUM_TAIL` still reads the SUMMARY.
The census counts only jobs that reported, and an unmeasured `times` prints `CPU unmeasured`, never
`0s`. The tag is `INTEGRATION` on every path, `--smoke` included, so the `sed` matches.
`cpu_ms`/`printf -v` are bash 3.2-safe. The line is additive, so it is a minor-bump shape.

## Criteria

| criterion | status | shown by |
|---|---|---|
| `test-shape --help` says readability gate and names what governs cost | met | `check test-shape --help` lines 1, 10-18 |
| `integration.sh` reports boots and boot cost as a census line | met | `integration.sh` `boots_line`; `test_runners_installable.py` BOOTS regex |
| README testing section carries the cost model in one sentence | met | `README.md:267` |
| `integration`/`-all`/`-diff` pass `gdk_gate` a fifth argument | met | `Makefile.tiers` (installable) `$(GDK_CENSUS_BOOTS)` on all three, and `smoke` |
| `[tests] cases` on those tiers grades instead of UNCOUNTED | not verified | agentic-sdlc's `check budget` not run here |

Also noted:
- `N7` NIT `src/godot_devkit/godot/checks/test_shape.py:16`: the docstring lists
  `integration`/`integration-all`/`integration-diff` as filing boots. `smoke` does too (CHANGELOG,
  `Makefile.tiers`).

```text
verdict: SHIP
| id | severity | disposition |
| N7 | NIT | rejected: a NIT, left as written for 1.1.0 |
```
