# 1.1.0 — the-two-pin-instruction-is-complete: feature review

Reviewer run 2026-09-12 over `547cb8b..f149a3f` (lane commit `d184cd9`). Method: I read README
§ "Install — two pins" against the criterion and the proof budget.

**Verdict: SHIP-WITH-FIXES.** The section names the third step for a repo with a PM tree (`pm init`,
then `pm vocabulary`) and says in one sentence that the flow has no default. It also writes the
fresh-repo and bumping-consumer paths out separately.

## Blockers

None at MAJOR or above.

## Criteria

| criterion | status | shown by |
|---|---|---|
| names `pm init` (or `adopt`) and `pm vocabulary` for a repo with a tree | met | `README.md` § Install, the `make pm ARGS='init'` block |
| one sentence: no default flow, so the PM CLI is dead without it | met | "has no default, so a repo that skips this step has a working gate set and a `pm` that refuses every verb that moves work" |
| fresh-repo path distinguished from bumping-consumer path | met | the two bullets under the block |
| proof budget: 1 case holding the added command to being real | not met | `d184cd9` touches only the config census; no case names `pm init`/`vocabulary` |

Findings:
- `MN2` MINOR `README.md:28,37`. The "complete" install pins `DEVKIT_VERSION := v0.2.0`, but this
  milestone was built and proven on v0.11.0 (`a85e313`). A consumer following the section gets a
  kit 9 minors behind, and the budgeted case that would hold `pm vocabulary` to being real at the
  pinned tag never landed. Repro: `grep -n 'v0.2.0' README.md` gives lines 28 and 37;
  `grep DEVKIT_VERSION Makefile` gives `v0.11.0`. Fix: land the budgeted case, or amend the
  budget, and move the example pin in the release commit along with `GODOT_DEVKIT_VERSION`.

```text
verdict: SHIP-WITH-FIXES
| id | severity | disposition |
| MN2 | MINOR | landed 782cbee |
```
