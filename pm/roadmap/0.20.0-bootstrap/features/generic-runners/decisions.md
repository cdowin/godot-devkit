Append with `godot-devkit pm decide <grain-id>` — never by hand; the command stamps the date and the next ordinal.

# 0.20.0/generic-runners — decisions

## D1 — 2026-09-03 — closed at the release: the review record is the milestone's

Shipped in v0.20.0 (tag `v0.20.0`, release gate review record `d9a830c` at the milestone grain)
and carried through v0.21.0 (`v0.21.0`) without a feature-level close. Closed 2026-09-03 while
cutting v0.22.0, which the release protocol refuses over a live milestone. No finding beyond the
milestone record. **Would have asked? NO** — housekeeping.
