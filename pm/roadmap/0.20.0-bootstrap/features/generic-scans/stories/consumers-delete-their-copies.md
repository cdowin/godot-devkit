---
id: 0.20.0/generic-scans/consumers-delete-their-copies
feature: 0.20.0/generic-scans
milestone: "0.20.0"
name: both consumers delete the shell copies and keep only their own scans
status: todo
owner:
depends_on: []
---

# both consumers delete the shell copies and keep only their own scans

## Goal
Both consumers delete the six shell copies + allowlist files, carry their exceptions in `devkit.toml`, and `make check` is green with the same findings count as before.
## Commit prefix
`feat(0.20.0/generic-scans/S3):`
## Size
s
